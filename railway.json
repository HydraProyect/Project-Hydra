{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "dotnet CaeManager.Web.dll --migrate-only",
    "healthcheckPath": "/salud",
    "healthcheckTimeout": 300
  }
}
