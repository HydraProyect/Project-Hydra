# Roadmap — CAE Manager

Orden de construcción por dependencia real de dominio, no por número de módulo del brief. Cada fase tiene criterios de aceptación verificables; no se empieza la siguiente fase sin cerrar los de la anterior.

## Fase 0 — Fundación

Sin UI de negocio todavía visible; es la base sobre la que se apoya todo lo demás.

- Solución .NET 9 con las 4 capas (`Domain`, `Application`, `Infrastructure`, `Web`) y proyectos de test, según `ARCHITECTURE.md`.
- Modelo de datos completo (todas las entidades de `DATABASE.md`) con `IEntityTypeConfiguration`, migraciones aplicadas, datos semilla de `TipoDocumento` y `ParametroSistema` reales (los 15 tipos y los umbrales 30/15 del Excel).
- ASP.NET Core Identity configurado: login, logout, roles semilla (`Administrador`, `Supervisor`, `EjecutivoCae`, `Consulta`), policies de autorización base.
- Interceptor de auditoría + soft delete global query filter funcionando sobre al menos una entidad de prueba.
- Design tokens de `DESIGN_SYSTEM.md` como variables CSS globales + layout shell (Navbar, Sidebar, área de contenido) con modo claro/oscuro.
- Componentes core implementados y documentados en `DESIGN_SYSTEM.md`: Button, Input, Select, Badge, Card, DataTable básica, Empty State, Loading State, Toast, Modal.
- Dashboard con los seis KPIs del Excel real (Trabajadores activos, Centros/Plataformas, Documentos vencidos, Urgentes, Próximos, Vigentes) — puede mostrar datos semilla/demo en esta fase.

**Criterio de aceptación**: la aplicación arranca, un usuario puede iniciar sesión, ve el Dashboard con KPIs reales de la base de datos (no hardcodeados), y existen pruebas unitarias pasando para el cálculo de estado de Documento.

## Fase 1 — Módulo vertical de referencia: Clientes

Primer módulo de negocio completo, de punta a punta, que fija el patrón que **todos** los módulos siguientes replican literalmente.

- CRUD completo de Cliente: listar (tabla con búsqueda, orden, filtro por `EsCritico`, paginación), crear (drawer), editar, eliminar (soft delete con confirmación), ver detalle.
- Los 5 estados de página (`Loading`/`Empty`/`Error`/`Forbidden`/`Success`) implementados y verificados manualmente, no solo en teoría.
- Auditoría visible: el detalle de un Cliente muestra su historial de cambios.
- Exportar la tabla de Clientes a Excel (primer caso real de exportación, patrón reutilizable).

**Criterio de aceptación**: un usuario puede crear, buscar, editar, exportar y eliminar (soft) un Cliente sin fricción, en menos de 3 clics desde el Dashboard, con feedback claro en cada paso. Este módulo sirve de plantilla — se documenta explícitamente qué archivos/carpetas copiar como punto de partida para el siguiente módulo.

## Fase 2 — Cadena de dominio principal

En orden de dependencia (cada uno depende del anterior):

1. ✅ **Empresas** — catálogo simple, mismo patrón que Clientes.
2. ✅ **Centros** — depende de Cliente; CRUD base con selector de Cliente. `PlataformaAcceso` (credenciales cifradas) y `RequisitoDocumental` se añaden en una iteración posterior sobre el detalle de Centro, cuando ese módulo pase a página completa con secciones — no bloquean el resto de Fase 2.
3. ✅ **Trabajadores** — depende de Empresa, con validación de DNI.
4. ✅ **Tipos de Documento** — pantalla de administración sobre el catálogo semilla de Fase 0, restringida al rol Administrador (crear/editar, sin eliminar — es catálogo de configuración).
5. ✅ **Documentos** — depende de Trabajador + TipoDocumento. Subida de PDF (`IFileStorageService` sobre disco, fuera de `wwwroot`, servido solo vía endpoint autenticado) y cálculo de estado en vivo con `CalculadoraEstadoDocumento` — el mismo cálculo que usa el Dashboard, para que nunca haya dos vigencias distintas mostradas en la app. "Editar" un documento es renovarlo (nueva fecha de emisión); trabajador y tipo de documento no cambian una vez creado.
6. **Documentos Empresa** — variante a nivel Empresa en vez de Trabajador, si el Excel/negocio lo confirma como necesario al llegar aquí (revisar contra datos reales antes de construir). Pendiente — no bloquea el resto de Fase 2.
7. ✅ **Asignaciones** — depende de Trabajador + Centro. `Asignacion` no tiene soft delete en el dominio: "eliminar" es darla de baja (`FechaBaja`), preservando el historial completo — mejora directa sobre la matriz de "X" del Excel, que no guardaba fechas. La advertencia de bloqueo de acceso por `RequisitoDocumental` queda pendiente de ese módulo (ver punto 2).
8. ✅ **Alertas** — vista de solo lectura calculada en vivo sobre Documentos (Próximo/Urgente/Vencido, ordenada por severidad), no una tabla `Alerta` sincronizada por un job en segundo plano — todavía no existe infraestructura de jobs programados en el proyecto, y una vista calculada nunca puede desincronizarse del estado real. La entidad `Alerta` del dominio (con `Nivel` y `FechaGeneracionUtc`) queda preparada para cuando se necesite marcar alertas como leídas por usuario o notificar activamente; hoy no se usa. El mapeo estado→color/etiqueta se centralizó en `EstadoDocumentoUi` para que Documentos y Alertas nunca puedan mostrar el mismo estado de forma distinta.

Empresas, Centros, Trabajadores, Tipos de Documento, Documentos, Asignaciones y Alertas replican el patrón de Clientes (repositorio de agregado + Commands/Queries en Application + página con QuickGrid/Drawer/DialogoConfirmacion en Web, donde aplica) y quedaron verificados end-to-end en navegador — incluyendo, para Documentos, subida y descarga real de un PDF verificada byte a byte y el semáforo de vigencia calculado correctamente contra la fecha de emisión; para Asignaciones, el flujo completo Cliente→Centro→Empresa→Trabajador→Asignación→Baja; y para Alertas, que un documento vencido creado en Documentos aparece correctamente listado. El estilo de página de listado se extrajo a `wwwroot/css/list-page.css`. La cultura de la aplicación se fijó a `es-ES` (fechas/números) al detectar que sin configurarla explícitamente se renderizaban en formato estadounidense.

**Fase 2 completa.** El flujo completo real del Excel (dar de alta un trabajador, asignarlo a un centro, cargarle su documentación, ver su semáforo de vigencia y sus alertas) es reproducible en el sistema de principio a fin. Solo queda pendiente Documentos Empresa, condicional a confirmación de negocio — no bloquea el paso a Fase 3.

**Criterio de aceptación**: el flujo completo real del Excel (dar de alta un trabajador, asignarlo a un centro, cargarle su documentación, ver su semáforo de vigencia) es reproducible en el sistema de principio a fin.

## Fase 3 — Vistas transversales y operación diaria

- ✅ **Dashboard** con datos 100% reales y desglose por rol. Los 6 KPI base los ve cualquier rol. A partir de ahí, escalera aditiva por rol (cada uno ve lo del anterior más lo suyo): EjecutivoCae añade "Documentos que requieren atención" (top 5 vencidos/urgentes, mismo cálculo que Alertas); Supervisor añade "Centros con más riesgo" (conteo de vencidos/urgentes agrupado por Centro, vía asignaciones activas); Administrador añade "Empresas con más riesgo" (mismo conteo agrupado por Empresa contratista). Consulta ve solo los KPI base. El rol se resuelve leyendo `AuthenticationStateProvider` en el componente (no hay reglas de negocio de Application involucradas — es una decisión de presentación). Verificado en navegador con los 4 roles: cada uno ve exactamente las secciones que le corresponden y ninguna de las que no.
- ✅ **Calendario** de vencimientos: vista mensual (semana en lunes, convención española), con navegación mes anterior/siguiente y botón "Hoy". Cada día con vencimientos muestra una insignia con el recuento coloreada según el peor semáforo de ese día (mismo cálculo que Dashboard/Alertas); clic en el día abre un modal con el detalle (trabajador, tipo de documento, estado). `ObtenerVencimientosMesQuery` en Application filtra `Documento.FechaVencimiento` dentro del mes pedido. Verificado en navegador: carga del mes actual, navegación entre meses, botón "Hoy", clic en un día con datos muestra el detalle correcto, cierre del modal.
- ✅ **Reportes**: página `/reportes` con vista previa en pantalla (misma tabla/badges que Alertas) y exportación a Excel y PDF del reporte de vigencia documental — a diferencia de Alertas, incluye también los documentos Vigentes, porque es el reporte pensado para entregar a un cliente/auditor como prueba de cobertura completa, no solo lo pendiente de acción. Excel con ClosedXML (mismo patrón que Clientes, Fase 1). PDF con **PDFsharp 6** (MIT) en vez de QuestPDF (licencia por ingresos de la empresa que lo usa, inviable para un SaaS de terceros — ver `ARCHITECTURE.md`), dibujando la tabla a mano con `XGraphics` y fuente DejaVu Sans embebida como recurso del ensamblado (necesario para que PDFsharp, que no depende del SO, renderice bien los acentos del español). Verificado en navegador: vista previa con datos reales, descarga de Excel y de PDF (encabezado `%PDF-` válido, tamaño de archivo coherente, texto y acentos legibles al releer el PDF generado).

- ✅ **Buscador global** (`Cmd/Ctrl+K`) sobre Cliente/Empresa/Centro/Trabajador — reemplazo directo de la hoja "Filtros" del Excel. Overlay con búsqueda con debounce agrupada por categoría; cada resultado navega al listado del módulo con el filtro precargado (`?q=`), no a una página de detalle (todavía no existen). Verificado en navegador: atajo de teclado, clic en botón visible, búsqueda por nombre y por DNI, navegación con filtro precargado, cierre con Escape y al navegar.

  Construir esto destapó un bug real y significativo: **los toasts llevaban toda la Fase 2 sin funcionar.** El render mode de una página (`@rendermode InteractiveServer`) no se propaga hacia arriba a su `MainLayout` — todo lo que vive en el Layout fuera de `@Body` (como `<AnfitrionToasts />`) permanecía estático y nunca recibía eventos. Ningún test lo detectó porque las aserciones comprobaban la presencia del contenedor `.toast` en el HTML, no de un mensaje real. Corregido dando `@rendermode InteractiveServer` explícito a cada componente del Layout que lo necesita (ver `CODING_STANDARDS.md`). De paso se corrigió otro bug real: la `FallbackPolicy` de autorización exigía sesión iniciada también para los archivos estáticos (JS/CSS), causando una carrera real en navegaciones frescas — los `import()` dinámicos de JS no se reintentan solos. Los archivos estáticos ahora son `AllowAnonymous()`.

**Fase 3 completa.** Las cuatro vistas transversales (Dashboard con desglose por rol, Calendario, Reportes, Buscador global) están construidas y verificadas end-to-end en navegador con datos reales.

**Criterio de aceptación**: cada rol ve un Dashboard con la información que le corresponde, se puede navegar el calendario de vencimientos mes a mes, se puede exportar el estado documental completo a Excel y PDF, y se puede encontrar cualquier Cliente/Empresa/Centro/Trabajador desde cualquier pantalla en menos de tres clics (`Cmd/Ctrl+K`).

## Fase 4 — Administración y gobierno

Las cuatro pantallas de esta fase están restringidas al rol Administrador (`@attribute [Authorize(Roles = ...)]`, mismo patrón que Tipos de Documento en Fase 2).

- ✅ **Configuración**: `/configuracion` edita `ParametroSistema.UmbralAmbarDias`/`UmbralRojoDias` (antes fijos en el seed de Fase 0). Reutiliza las invariantes que el dominio ya protegía (`ParametroSistema.Actualizar`, ámbar > rojo > 0) a través de un `IParametroSistemaRepository` nuevo — fila única, sin Id explícito en la interfaz. Verificado en navegador: valores semilla 30/15 al cargar, guardar cambios, persistencia tras recargar, y que la validación de negocio (ámbar > rojo) bloquea correctamente un valor inválido sin escribir en la base de datos.
- ✅ **Auditoría** como módulo navegable en `/auditoria` (antes solo se acumulaba en la tabla `RegistroAuditoria` sin ninguna pantalla). `RegistrosAuditoria` se expuso en `IApplicationDbContext`; `ObtenerAuditoriaQuery` pagina y filtra por tipo de entidad. Application no resuelve el nombre de usuario (no conoce Identity, que vive en Infrastructure) — Web lo resuelve después vía `UserManager`, mismo principio que ya separa Login del resto de la app. Verificado en navegador: carga de registros reales generados por otras acciones de esta misma fase, filtro por tipo de entidad, paginación.
- ✅ **Usuarios**: `/usuarios`, CRUD completo sobre Identity directamente (`UserManager`/`RoleManager`), sin pasar por MediatR/Application — Identity ya es su propio sistema de persistencia, igual que Login ya hacía. Crear, editar nombre/rol, y activar/desactivar (vía `LockoutEnd`, no eliminación — coherente con el soft delete del resto de la app). Verificado en navegador, incluyendo dos casos de seguridad reales: un usuario desactivado no puede iniciar sesión, y un Administrador no puede desactivar su propia cuenta.
- ✅ **Roles**: `/roles`, vista de referencia (no CRUD dinámico) — los 4 roles están fijos y alineados 1:1 a los 4 niveles de Dashboard (ver `ARCHITECTURE.md`), así que la pantalla muestra su descripción y cuántos usuarios tiene cada uno, con enlace a Usuarios para la gestión real. Verificado en navegador.

**Fase 4 completa.**

**Criterio de aceptación**: un Administrador puede dar de alta un usuario con un rol, verificar que ese rol determina lo que ve en Dashboard, desactivar y reactivar una cuenta, ajustar los umbrales de alerta sin tocar código, y ver el historial completo de auditoría filtrado por entidad.

## Fase 5 — Importación desde Excel

✅ Completa. Se abordó al final a propósito: solo tenía sentido una vez existían todas las entidades destino. Verificada end-to-end con `CAE_KHS_Cuadro_de_Control_2.xlsx` real (no un fixture sintético) — el archivo **no se incorporó al repositorio** porque contiene datos personales reales (nombres, DNI, fecha de nacimiento, email de trabajadores reales de KHS); la verificación se hizo subiéndolo directamente en un navegador real vía Playwright contra una base de datos local descartable, nunca commiteado.

- Página `/importacion` (Administrador): analizar (sin escribir nada) → revisar el plan → confirmar → reporte final. `IExcelImportacionParser` (Application) implementado con ClosedXML en Infrastructure (`ClosedXmlImportacionParser`), igual patrón de dependencia invertida que `IFileStorageService`.
- Hojas leídas: `Centros_Plataformas`, `Empleados`, `Extranjeros (KHS GmbH)`, `Asignaciones`. `Requisitos_Centro` y las columnas de `PlataformaAcceso` (Plataforma/Usuario/Contraseña) se omiten deliberadamente — ver Fase 2, punto 2: ese módulo de UI todavía no existe, importar esos datos sin pantalla para verlos ni gestionarlos sería peor que no importarlos. `Parametros` no se reimporta: ya quedó sembrado en Fase 0 con los mismos valores exactos de este archivo.
- Cada fila de `Centros_Plataformas` se importa como un Cliente **y** un Centro con el mismo nombre (el Excel origen fusiona ambos conceptos en una sola columna de texto libre sin separador fiable — el propio dominio ya documentaba esta normalización, ver `Centro.cs`). Los nombres que indican centro genérico, sin confirmar, o que fusionan varios centros reales (p. ej. "Coca-Cola European Partners (genérico - planta sin confirmar)", "Mahou - San Miguel (todos los centros: ...)") se importan tal cual pero se marcan como **advertencia**, con instrucción explícita de revisar y desglosar manualmente después — no se intenta adivinar el desglose.
- Detección de errores explícita, con motivo, sin abortar el resto del archivo: DNI vacío o con formato inválido, DNI duplicado dentro del propio archivo, fecha de emisión futura, tipo de documento no reconocido en el catálogo, fila de trabajador sin nombre/apellidos. Cada intento de escritura en la base de datos va además protegido con try/catch por fila — un dato inesperado no aborta el resto de una importación por lo demás válida.
- Reutiliza entidades ya existentes por nombre/DNI en vez de duplicarlas: **reimportar el mismo archivo no crea filas repetidas** (verificado explícitamente).

Dos bugs reales encontrados al verificar contra el archivo real:

1. **Falso rechazo de asignaciones por nombre.** La hoja `Asignaciones` usa nombres abreviados en la cabecera de cada columna de centro (p. ej. "COBEGA"), mientras que `Centros_Plataformas` usa el nombre completo ("COBEGA (Coca-Cola European Partners) - Plantas Vallès / Tacoronte"). El emparejamiento por texto exacto descartaba silenciosamente la mayoría de asignaciones reales como advertencia. La propia hoja ya documentaba la solución en su nota introductoria ("las columnas corresponden al mismo orden que la hoja Centros_Plataformas") — se cambió a emparejar por **posición**, verificado 1:1 contra las 36 columnas/filas reales, con una comprobación defensiva que aborta con un mensaje claro si algún día el número de columnas no coincide con el de filas (en vez de emparejar mal en silencio). Esto casi triplicó las asignaciones detectadas (de 71 a 215).
2. Un trabajador del Excel real tiene un DNI en formato NIE (`Z2048304K`, extranjero) en la hoja `Empleados`, no `Extranjeros`. `Trabajador.EstablecerDni` (Fase 2) solo acepta el formato DNI (`\d{8}[A-Z]`), no NIE — se omite correctamente con motivo explícito en el reporte en vez de fallar la importación completa. Soportar NIE es una mejora de dominio real y legítima, pero fuera de alcance de esta fase (afectaría a todo el módulo Trabajadores, no solo a la importación) — queda anotado para v1.1.

**Criterio de aceptación**: el Excel real de KHS se importa completo (36 Clientes, 36 Centros, 1 Empresa, 24 Trabajadores, 242 Documentos, 215 Asignaciones), con un reporte claro de qué se importó, qué se omitió y por qué (4 advertencias, 4 omitidos, cada uno con motivo). Verificado también que reimportar el mismo archivo no duplica nada.

## Fase 6 — Rediseño de identidad visual ProjectHydra

✅ Completa (primera pasada). Tras cerrar las 5 fases de v1, se recibió el brief de marca "ProjectHydra Platform Redesign" — realinear la identidad visual con ProjectHydra (60% identidad ProjectHydra, 20% estructura Stripe Dashboard, 10% minimalismo Notion, 10% pulido Linear), sin tocar arquitectura ni funcionalidad. Ver `DESIGN_SYSTEM.md` para el detalle completo de tokens; aquí solo el resumen de qué se tocó y qué queda pendiente.

- **Tokens** (`tokens.css`): nueva paleta (primario navy `#0D4E89`, secundario teal `#2FB8C6`), escala tipográfica H1–H4/Body/Small/Caption explícita, radios reestructurados en 5 niveles (`sm` 8px / `md` 12px / `card-sm` 16px / `lg` 20px / `full`), sombras mucho más suaves (`0 8px 30px rgba(15,23,42,.05)`), tokens de `motion` (150/200/250ms) nuevos. Los **nombres** de variable se mantuvieron estables — más de 50 archivos ya las consumían — solo cambiaron los valores, evitando una migración masiva y el riesgo que conlleva.
- **Componentes restilizados**: Boton (padding 14×24, radio 12px, elevación sutil en hover), CampoTexto/CampoSelect/CampoTextarea (min-height 48px, radio 12px, foco con outline sólido + glow suave — el outline sigue siendo el indicador accesible real, el glow es decorativo), Tarjeta (radio 20px), TarjetaMetrica (radio 16px), Toast (radio 16px + entrada con fade/slide), tablas (`list-page.css`: filas más espaciosas, cabecera sticky en mayúsculas con tracking, hover suave).
- **Iconografía**: primer set de iconos outline del proyecto (`Icono.razor`, SVG inline propio, sin librería externa — trazo 1.75px estilo Lucide/Heroicons), integrado en el sidebar (16 iconos) y en el botón de búsqueda global. Antes no había ningún icono en la navegación.
- **Verificado visualmente en navegador**: Login, Dashboard, un listado con QuickGrid (Clientes), un Drawer de creación con foco visible, Reportes, Calendario, el overlay del buscador global.

Un bug real encontrado al verificar: **los inputs de la pantalla de Login llevaban toda la vida del proyecto sin ningún estilo aplicado** (altura nativa del navegador ~21px, sin radio). Causa: `Login.razor` usa `<InputText>` de Blazor (componente hijo), y su CSS aislado (`Login.razor.css`) seleccionaba `input` directamente sin `::deep` — la CSS isolation de Blazor solo aplica el atributo de scope a elementos escritos directamente en el propio archivo `.razor`, no a los que renderiza un componente hijo por dentro. El resto de formularios de la app nunca tuvo este problema porque usan los componentes propios `CampoTexto`/`CampoSelect` (que sí escriben su `<input>`/`<select>` directamente). Corregido añadiendo `::deep` a los selectores de Login.razor.css.

Pendiente para una siguiente pasada (no bloquea, documentado en `DESIGN_SYSTEM.md` → "Pendientes"): componentes Chip, Tooltip, Tabs, Breadcrumb y un componente de Charts real (Dashboard hoy solo tiene KPIs numéricos y tablas, sin visualización gráfica); selector de tema claro/oscuro visible en la UI (los tokens ya están preparados para `prefers-color-scheme`, falta el control); guía de fotografía corporativa (no aplica todavía — la app no tiene pantallas de marketing).

## Fase 7 — Ajustes de usabilidad post-lanzamiento

✅ Completa. Cinco peticiones puntuales tras usar la app en el día a día.

- **Plantilla de importación masiva de Clientes** (`/clientes/importar`, Administrador): a diferencia del importador de Fase 5 (multi-hoja, específico del Cuadro de Control CAE de KHS), esta es una plantilla de una sola hoja (`Cliente / Centro`, `Crítico`, `Dirección`, `Contacto`) pensada para dar de alta rápidamente el resto de la cartera de clientes. Reutiliza `EjecutarImportacionCommand` de Fase 5 sin cambios (ya trataba `ClientesCentros` de forma genérica) — solo se añadió un parser nuevo (`ClosedXmlPlantillaClientesService`) y un endpoint de descarga de plantilla en blanco (`/clientes/plantilla.xlsx`, con una fila de ejemplo marcada para borrar). Mismo flujo Analizar → revisar plan → confirmar → reporte que Fase 5.
- **Espaciadores en el menú lateral**: gap entre ítems del sidebar de 4px a 8px.
- **Trabajadores por Empresa sin pasar por el buscador de texto**: se añadió un selector `Empresa` (dropdown estructurado, no búsqueda) a la barra de filtros de `/trabajadores`, con su propia Query (`ObtenerTrabajadoresQuery.EmpresaId`). Antes solo existía la búsqueda libre por nombre/apellidos/DNI, sin ninguna forma de ver la plantilla completa de una empresa concreta.
- **Búsqueda de trabajador en tiempo real**: verificado que ya funcionaba sin necesidad de Enter (el `oninput` de `CampoTexto` ya dispara la búsqueda en cada pulsación) — no requirió cambio de código, solo verificación explícita en navegador.
- **Paginación en todas las páginas de listado**: auditadas todas — Clientes/Empresas/Centros/Trabajadores/Documentos/Asignaciones ya tenían QuickGrid+Paginator (Fase 1-2). Se añadió paginación (en memoria, sobre la lista ya cargada — estos módulos no tenían Query paginada en servidor y el volumen de datos no lo justifica) a Tipos de Documento, Alertas, Usuarios y la vista previa de Reportes, con un componente nuevo reutilizable `PaginadorSimple.razor` (Anterior/Siguiente + "Página X de Y") que también reemplazó la implementación duplicada que ya tenía Auditoría. Sin cambios en Roles (4 tarjetas fijas, no es una lista dinámica) ni en Auditoría (ya paginaba, solo se centralizó su UI).

## Fase 8 — Menú lateral como botones + progreso con mensajes en importaciones

✅ Completa. Dos peticiones de pulido tras usar la app: el sidebar "se veía horrible" y las importaciones (Fase 5 y Fase 7) no comunicaban nada durante los segundos de espera.

- **Menú lateral rediseñado como botones**: `NavMenu.razor.css` reescrito — radio de `--radius-sm` (8px) a `--radius-md` (12px), borde transparente que se revela en hover, sombra suave en hover, y feedback táctil real con `.nav-item:active { transform: scale(0.97); transition-duration: 80ms; }` (el ítem "cede" ligeramente al pulsar y suelta al soltar). Se quitó el acento de barra izquierda (`::before`) del estado activo en favor de un fondo tintado (look de "botón seleccionado"), consistente con el resto del sistema de diseño.
  - **Bug real encontrado al verificar**: los selectores `.nav-item` no llevaban `::deep`, así que nunca aplicaron — `<NavLink>` es un componente hijo de Blazor (como `<InputText>` en el bug ya documentado de `Login.razor` en Fase 6) y el `<a>` que renderiza internamente no recibe el atributo de scope de CSS isolation de `NavMenu.razor`. El sidebar llevaba toda la vida del proyecto como enlaces sin estilo (azules, subrayados, sin iconos alineados). Corregido reescribiendo todos los selectores como `.nav-principal ::deep .nav-item...` (ligados al `<nav class="nav-principal">`, que sí es una etiqueta literal de este componente). Verificado con Playwright: `getComputedStyle(...).borderRadius` pasó de `0px` a `12px`.
- **Barra de progreso con mensajes rotativos** (`ProgresoConMensajes.razor`, nuevo componente en `Components/DesignSystem/`): barra indeterminada (no finge un porcentaje real, el backend no reporta progreso granular) + mensaje contextual que rota cada 1.4s mientras la operación está en curso (`System.Timers.Timer` + `InvokeAsync(StateHasChanged)`, con `@media (prefers-reduced-motion: reduce)` respetado). Sustituye al esqueleto genérico `EstadoCargando` en los dos flujos de importación:
  - `/importacion` (Fase 5): "Leyendo el archivo Excel…", "Revisando Clientes y Centros…", "Comprobando Empleados y Documentos…", "Verificando Asignaciones…" al analizar; "Creando Clientes y Centros…", "Dando de alta Trabajadores…", "Registrando Documentos…", "Asignando Trabajadores a sus Centros…" al confirmar.
  - `/clientes/importar` (Fase 7): mensajes equivalentes, más breves, acordes al alcance más simple de esa plantilla.
  - Verificado end-to-end con un archivo de prueba de 300 filas (sin datos reales — generado localmente): se capturó en vivo la barra con el mensaje "Leyendo la plantilla…" durante el análisis, y la importación completó correctamente (300 Clientes + 300 Centros creados).

## Fase 9 — Validación real de documentos de identidad (DNI/NIE/CIF/TIE)

✅ Completa. Resuelve una limitación documentada desde Fase 5 ("Fuera de alcance: soporte de NIE") y una petición explícita del usuario tras probar en local: los campos de identificación no debían estar fijos a un único formato.

- **`ValidadorIdentificacion`** (nuevo, `CaeManager.Domain/Common/`): detecta y valida con dígito de control real DNI, NIE y CIF/NIF de empresa (algoritmo oficial español, aportado por el usuario), reconoce el número de soporte TIE por formato, y trata cualquier otro documento (pasaporte extranjero) como válido sin exigir dígito de control — igual que hace la referencia del usuario. 12 pruebas nuevas en `ValidadorIdentificacionTests`.
- **`Trabajador.Dni`**: ya no exige `\d{8}[A-Z]` — acepta DNI, NIE, TIE o cualquier documento extranjero; solo rechaza cuando el formato encaja con uno de dígito de control calculable (DNI/NIE/CIF) y el dígito no cuadra. Columna `Dni` ampliada de 9 a 20 caracteres (migración `InicialCompleta` regenerada — proyecto pre-lanzamiento, sin datos reales que migrar).
- **`CrearTrabajadorCommandValidator`** y el **parser de importación Excel de Fase 5** actualizados con la misma regla (antes tenían su propio regex de solo-DNI, duplicado e inconsistente).
- **Pista en vivo** en el formulario "Nuevo trabajador" (`/trabajadores`): mientras se escribe el documento, un mensaje bajo el campo indica el tipo detectado y si el dígito de control es correcto (✓/✗) o si se trata de un documento no español (ℹ, aceptado sin más).
- Verificado end-to-end en navegador: un trabajador con NIE (`X1234567L`) se crea y guarda correctamente — antes era rechazado por el formato fijo de DNI.

## Fase 10 — Modelo de datos: Cliente (Razón social + CIF), Empresa↔Cliente, Centro exige Empresa

✅ Completa. La pieza más grande del backlog post-lanzamiento: Cliente no tenía forma de identificarse fiscalmente, Empresa no tenía relación con Cliente, y Centro solo exigía Cliente — no reflejaba que una Empresa contratista trabaja para varios Clientes, ni que un Centro pertenece a ambos a la vez.

- **`Cliente`**: el campo `Nombre` se renombra a `RazonSocial` (es lo que realmente representa) y se añade `Cif` obligatorio, validado con dígito de control real (`ValidadorIdentificacion`, Fase 9) y con comprobación de duplicidad propia (`Cliente.CifDuplicado`, además del `RazonSocialDuplicada` ya existente). Columna `Cif` con índice único.
- **Empresa ↔ Cliente, varios a varios** (nuevo `EmpresaCliente`, tabla puente `EmpresasClientes`, sin ciclo de vida propio — desvincular es una baja física): el drawer de "Nueva empresa" ahora incluye una lista de checkboxes "Clientes con los que trabaja"; `CrearEmpresaCommand`/`EditarEmpresaCommand` sincronizan las asociaciones (añade las nuevas, borra las quitadas) en la misma transacción que guarda la Empresa.
- **`Centro` exige Cliente y Empresa**: al crear un Centro, tras elegir el Cliente el selector de Empresa se recarga y solo muestra las Empresas ya asociadas a ese Cliente (`ObtenerEmpresasParaSelectorQuery(ClienteId)`) — evita asociaciones incoherentes. Cliente y Empresa son inmutables tras crear el Centro (mismo criterio que `Trabajador.EmpresaId`).
- **Duplicados y búsquedas sin distinguir mayúsculas**: `Cliente.RazonSocial`, `Empresa.RazonSocial` y `Centro.Nombre` (dentro de su Cliente) se comparaban con `==` directo — en SQLite eso es sensible a mayúsculas, así que "Cliente A" y "CLIENTE A" se creaban como dos clientes distintos. Corregido en los tres repositorios (`.ToUpper() == .ToUpper()`) y en todos los buscadores de texto de la aplicación (Clientes, Empresas, Centros, Trabajadores, Documentos, Asignaciones, buscador global) que usaban `.Contains()` directo sobre el campo.
- **Importación por Excel (Fase 5 y Fase 7), degradación explícita**: ninguna de las dos plantillas recoge todavía CIF ni Empresa (se deja para una fase aparte, a petición del usuario). Como ahora son obligatorios, `EjecutarImportacionCommand` ya no puede crear Clientes/Centros nuevos desde Excel — en vez de fallar o inventar un CIF/Empresa falsos, cada fila que requeriría crear uno nuevo se omite con un motivo explícito ("Este cliente no existe todavía. Ahora requiere un CIF, que esta plantilla no recoge — créalo manualmente en Clientes."). Los Clientes/Centros que ya existían se siguen reutilizando con normalidad.
- Verificado end-to-end en navegador: Cliente con CIF creado y duplicado (en minúsculas) correctamente rechazado; Empresa asociada a un Cliente; al crear un Centro, el selector de Empresa mostró solo la Empresa asociada al Cliente elegido (no las demás); búsqueda de Clientes en minúsculas encontró un cliente en mayúsculas; importación de un cliente nuevo sin CIF completada con 0 creados y el mensaje explicativo esperado, sin errores. 58/58 pruebas automatizadas pasan (10 nuevas).

## Fase 11 — Bug: los formularios en drawer se cerraban solos al corregir un error

✅ Completa. Bug real reportado tras probar en local: "cuando se le da guardar y aparece un mensaje de error por algún formato, al intentar corregirlo se cierra automáticamente".

- **`Drawer`** (a diferencia de `Modal`, que ya tenía `CerrarAlHacerClicFuera` para no cerrarse de fondo durante `EnProgreso`) no tenía ningún guard: un clic en la superposición cerraba el formulario siempre, sin importar si había un error de validación visible. Se añadió el mismo parámetro `CerrarAlHacerClicFuera` a `Drawer.razor`.
- Las 8 páginas que usan `<Drawer>` (Clientes, Empresas, Centros, Trabajadores, Documentos, Asignaciones, Tipos de Documento, Usuarios) ahora lo pasan como `_mensajeErrorFormulario is null && !_guardando` (y además `_erroresCampo.Count == 0` en las 6 que también muestran errores de campo individuales vía FluentValidation) — mientras hay un error visible o se está guardando, un clic fuera del panel ya no lo cierra.
- **Primer intento de reproducción incompleto**: un CIF/DNI con formato inválido genera un error de *campo* (`_erroresCampo`, vía `FluentValidation.ValidationException`), no el banner superior (`_mensajeErrorFormulario`) — ese es justo el caso "error por algún formato" que describió el usuario, y el guard inicial (solo `_mensajeErrorFormulario`) no lo cubría. Verificado con Playwright: con el error de campo activo, un clic en la superposición ya no cierra el drawer; sin ningún error, sigue cerrando con normalidad (comportamiento intencional preservado).

## Fase 12 — Trabajadores: preselección de Empresa desde el filtro + confirmación multi-centro

✅ Completa. Petición del backlog: "si voy a trabajadores y tengo preseleccionado el filtro de una empresa se debe presuponer que el trabajador que quiero dar de alta es de esa empresa".

- **Preselección de Empresa**: `AbrirCrearAsync` en `/trabajadores` ahora inicializa el campo Empresa del drawer con `_filtroEmpresaId` en vez de vaciarlo siempre — si la lista ya está filtrada por una Empresa, "Nuevo trabajador" abre con esa misma Empresa ya seleccionada (el usuario puede cambiarla si hace falta, no es un campo bloqueado).
- **Multi-centro**: no requirió ningún cambio de código — `Asignacion` (Trabajador × Centro, Fase 2) ya permitía que un mismo Trabajador tuviera varias asignaciones activas simultáneas en Centros distintos; solo faltaba confirmarlo explícitamente.
- Verificado end-to-end en navegador: con el filtro de Trabajadores puesto en "Empresa Multi", "Nuevo trabajador" abrió con esa Empresa ya preseleccionada; una misma trabajadora se asignó con éxito a dos Centros distintos ("Centro Norte" y "Centro Sur") y ambas asignaciones aparecen activas a la vez en `/asignaciones`.

## Fase 13 — Documentos: vencimiento manual, aviso de vigencia anterior, glosario de Tipos de Documento e importación masiva

✅ Completa. Cuatro peticiones relacionadas del backlog post-lanzamiento, todas centradas en el ciclo de vida de un Documento.

- **`CalculadoraEstadoDocumento.Calcular` simplificado**: se eliminó el parámetro `aplicaVencimientoAutomatico` — el único criterio para calcular el semáforo (vigente/ámbar/rojo/vencido/no aplica) pasa a ser simplemente si `FechaVencimiento` tiene valor o no. Antes, un Documento de un Tipo sin vencimiento automático nunca participaba en el semáforo aunque tuviera una fecha de vencimiento cargada a mano — con esta simplificación, cualquier vencimiento (automático o manual) se trata igual en Dashboard, Alertas, Calendario y Reportes.
- **Vencimiento manual para Tipos de Documento sin vencimiento automático**: al subir o renovar un Documento de un Tipo con `AplicaVencimientoAutomatico = false`, el formulario ahora muestra un campo "Fecha de vencimiento" editable (antes no existía en absoluto para estos tipos) con un botón "Copiar fecha de emisión" al lado, para el caso común de documentos que vencen el mismo día que se emiten. Para los Tipos con vencimiento automático, el campo no se muestra — se sigue calculando siempre a partir de la vigencia en meses.
- **Aviso de vigencia anterior al renovar**: si al renovar un Documento la nueva fecha de emisión es anterior a la que ya tenía registrada, `Guardar` no escribe directamente — muestra un popup de confirmación ("La nueva fecha de emisión es anterior a la actual… comprueba que sea el archivo correcto") y solo continúa si el usuario confirma explícitamente. Validación en el momento de guardar, no al elegir el archivo.
- **Glosario de Tipos de Documento**: nuevos campos `Descripcion`, `CriteriosValidacion`, `SeSolicitaA` y `Observaciones` en `TipoDocumento`, editables desde `/tipos-documento`. Se muestran automáticamente como bloque de solo lectura en el formulario de Documentos en cuanto se selecciona (al crear) o se conoce (al renovar) el Tipo de Documento — el usuario ve qué criterios debe cumplir el archivo sin salir del formulario.
- **Importación masiva de Documentos** (`/documentos/importar`, Administrador): plantilla de una sola hoja (DNI, Tipo de documento, Fecha de emisión) — a diferencia de la importación de Fase 5/7, esta no da de alta Trabajadores ni Tipos de Documento nuevos, ambos deben existir ya; cada fila que no encuentra un Trabajador o un Tipo de Documento coincidente se omite con motivo explícito. Reutiliza `EjecutarImportacionCommand` sin cambios (ya trataba `Documentos` de forma genérica desde Fase 5).

Un bug real encontrado al verificar en navegador: `CampoTextarea` (a diferencia de `CampoTexto`) nunca tuvo parámetro `MensajeError` — al añadir `MensajeError` a los nuevos campos de glosario (que usan textarea) el circuito de Blazor lanzaba una excepción no controlada (`InvalidOperationException: ... does not have a property matching the name 'MensajeError'`) y cerraba la sesión del usuario en cuanto se abría el formulario de edición de un Tipo de Documento. Corregido añadiendo el mismo soporte de `MensajeError` (borde rojo + mensaje bajo el campo) que ya tenía `CampoTexto`.

Verificado end-to-end en navegador: Tipo de documento editado con las 4 nuevas notas de glosario y guardado correctamente; al crear un Documento de "Apto médico laboral" el glosario aparece automáticamente; al crear un Documento de "Formación 60h" (sin vencimiento automático) aparece el campo manual + botón copiar, y el Documento resultante participa correctamente en el semáforo (quedó "Vencido" con una fecha de vencimiento manual en el pasado); al renovar con una fecha de emisión anterior aparece el popup de confirmación y el guardado queda bloqueado hasta confirmar; la plantilla de Documentos se descarga con las 3 columnas esperadas y una importación de prueba (2 filas, sin datos reales) creó 1 Documento nuevo y omitió correctamente la fila con un DNI inexistente. 57/57 pruebas automatizadas pasan.

## Fase 14 — Alertas y Calendario: clic para gestionar el documento directamente

✅ Completa. Petición del backlog: al ver un documento vencido en Alertas o en el Calendario, poder ir directo a renovarlo sin tener que buscarlo manualmente en Documentos.

- **`/documentos` acepta `?documentoId=...` por query string** (`[SupplyParameterFromQuery]`): si se llega con ese parámetro, `OnInitializedAsync` abre automáticamente el drawer "Renovar documento" para ese Documento, exactamente igual que si se hubiera hecho clic en "Renovar" desde la propia lista.
- **Alertas** (`/alertas`) y el **detalle de día del Calendario** (`/calendario`) ganan una acción "Gestionar" por fila que navega a `/documentos?documentoId={id}` vía `NavigationManager` — ambas ya exponían `DocumentoId` en su DTO (`AlertaDto`, `VencimientoCalendarioDto`), no hizo falta ningún cambio en Application.
- Verificado end-to-end en navegador: un Documento con vencimiento manual en 5 días aparece como "Urgente" tanto en Alertas como en el Calendario; "Gestionar" desde cualquiera de las dos pantallas navega a Documentos y abre el drawer de renovación ya relleno con los datos correctos del mismo Documento (mismo `documentoId` en ambos casos).

## Fase 15 — Tipos de Documento: filtro por Cliente/Empresa/Centro

✅ Completa. Petición del backlog, acotada explícitamente por el usuario a "solo filtro en la lista" tras una pregunta de aclaración — no cambia qué documentos se piden a quién ni genera alertas nuevas de "documento faltante" (eso queda fuera de alcance de v1, ver más abajo).

- **`TipoDocumentoCentro`** (nuevo, `CaeManager.Domain/Documentos/`): asociación opcional Tipo de Documento ↔ Centro, mismo patrón sin ciclo de vida que `EmpresaCliente` (Fase 10) — tabla puente `TiposDocumentoCentros`, índice único compuesto. Un Tipo de Documento **sin ninguna** asociación sigue siendo global (comportamiento de todo el catálogo semilla, sin cambios): aparece con cualquier filtro. Uno **con** asociaciones solo aparece cuando el filtro coincide con uno de sus Centros.
- **Filtro en cascada Cliente → Empresa → Centro** en `/tipos-documento`, mismo patrón visual que el selector de Centro ya usado al crear un Centro (Fase 10): elegir Cliente carga sus Empresas, elegir Empresa carga sus Centros. `ObtenerTiposDocumentoQuery` acepta los tres filtros opcionalmente y `ObtenerCentrosParaSelectorQuery` se extendió con `ClienteId`/`EmpresaId` opcionales (antes solo devolvía la lista completa).
- **Checklist "Centros donde aplica"** en el drawer de crear/editar Tipo de Documento (vacío = aplica en todos), mismo componente visual `.lista-seleccion-multiple` que ya usa Empresa para sus Clientes asociados (Fase 10). `CrearTipoDocumentoCommand`/`EditarTipoDocumentoCommand` sincronizan las asociaciones igual que `EditarEmpresaCommand` lo hace con `EmpresaCliente`.
- Verificado end-to-end en navegador: "Carretillas elevadoras" restringido a un Centro concreto aparece al filtrar por ese Centro (junto con los tipos globales, p. ej. "Apto médico laboral") y desaparece al filtrar por un Centro distinto — los tipos globales siguen apareciendo en ambos casos. 57/57 pruebas automatizadas pasan.

## Fase 16 — Documentos: conversión automática de imágenes a PDF + unificar varios archivos en una subida

✅ Completa. Petición del backlog, acotada tras dos preguntas de aclaración: solo imágenes JPG/PNG (no Word/Excel, que exigiría LibreOffice instalado en el servidor — fuera de alcance), y "Unificar" significa combinar varios archivos elegidos en una misma subida en un único PDF, no combinar Documentos ya guardados.

- **`ConversorArchivosPdf`** (nuevo, `CaeManager.Web/Documentos/`): usa PdfSharp directamente (ya era una dependencia del proyecto para `GeneradorPdfReporteDocumentos`, Fase reportes — mismo criterio documentado ahí sobre por qué no QuestPDF). Convierte una imagen JPG/PNG en una página A4 con la imagen centrada y ajustada sin deformar ni recortar, y combina una lista de archivos (PDF y/o imagen, en cualquier orden) en un único PDF multipágina.
- **`Documentos.razor`**: el campo de archivo pasa a aceptar PDF, JPG y PNG, y admite selección múltiple (`multiple`). `ManejarArchivoSeleccionadoAsync` valida tipo y tamaño de cada archivo (10 MB por archivo), los combina con `ConversorArchivosPdf.Unificar` y guarda el resultado como el único archivo adjunto del Documento — nunca se guardan varios archivos por Documento, siempre uno ya unificado.
- Verificado end-to-end en navegador: subir una foto JPG y una PNG a la vez en "Nuevo documento" produce un único PDF de 2 páginas (confirmado descargando el PDF resultante y renderizándolo — página 1 con el contenido de la JPG, página 2 con el de la PNG, cada imagen centrada y sin deformar).

## Fase 17 — Auditoría: ver el archivo anterior de un Documento renovado

✅ Completa. Petición del backlog ("retener documentos renovados por un periodo configurable"), investigada y acotada tras una pregunta de aclaración: el archivo antiguo de un Documento **nunca se borra hoy** al renovar (queda huérfano en disco indefinidamente) y sus datos ya quedaban en el registro de Auditoría — el problema real no era la retención, sino que no había forma de **verlo**. Se resolvió eso; la purga automática tras un periodo configurable queda fuera de alcance (exigiría la primera infraestructura de tareas programadas del proyecto).

- **`ObtenerRegistroAuditoriaPorIdQuery`** (nuevo): lee un registro de auditoría por Id — hasta ahora solo existía la versión paginada.
- **`/auditoria/{id}/archivo-anterior`** (nuevo endpoint, solo Administrador): lee el `DatosAntes` de un registro `Documento`/`Modificado` (JSON que `AuditoriaInterceptor` ya guardaba desde el principio), extrae el `ArchivoUrl` que tenía antes de la renovación y lo sirve como PDF.
- **`/auditoria`**: cada fila de una renovación de Documento con archivo anterior muestra un enlace "Ver archivo anterior"; las filas de alta ("Creado") nunca lo muestran, porque no había ningún archivo previo que ver.
- Verificado end-to-end en navegador: un Documento creado con una foto, luego renovado con una foto distinta — en Auditoría, la fila "Modificado" tiene el enlace y al abrirlo sirve el PDF **anterior** (tamaño y contenido distintos del PDF actual, confirmado descargando ambos), mientras que la fila "Creado" no muestra ningún enlace.

## Fase 18 — Importación combinada de Cliente + Empresas + Centros + Trabajadores

✅ Completa. Cierra el hueco que Fase 10 dejó explícitamente pendiente: ni la importación de Fase 5 (Cuadro de Control CAE de KHS) ni la plantilla simplificada de Fase 7 pueden crear Clientes o Centros nuevos desde entonces, porque ninguna de las dos recoge CIF ni Empresa. El alcance del modo de fusión se acotó tras una pregunta de aclaración.

- **Plantilla de 4 hojas** (`/clientes/importar-combinado`, `/clientes/plantilla-combinada.xlsx`, Administrador): Clientes (Razón social, CIF, Crítico), Empresas (Razón social, Clientes asociados separados por `;`), Centros (Nombre, Cliente, Empresa, Código, Dirección, Contacto, Contrato vigente hasta) y Trabajadores (Nombre, Apellidos, DNI, Empresa, Fecha de nacimiento, Email). Las referencias entre hojas son por nombre, no por Id — se resuelven contra la base de datos y contra las hojas anteriores del propio archivo, con un motivo explícito por fila cuando no se encuentra ("No se encontró el cliente/empresa \"X\" (ni en el sistema ni en la hoja Y de este archivo)").
- **Modo de fusión, elegible al confirmar** (`EjecutarImportacionCombinadaCommand`, parámetro `ReemplazarExistentes`): para una fila que coincide con un registro ya existente (por CIF en Clientes, por razón social en Empresas/DNI en Trabajadores, por Cliente+Nombre en Centros), *"Solo rellenar lo que falta"* (por defecto, el modo seguro) nunca sobrescribe un dato ya presente y solo añade asociaciones Empresa↔Cliente nuevas; *"Reemplazar con los datos del archivo"* sobrescribe los campos informados y sincroniza las asociaciones exactamente como vengan en el archivo (quita las que ya no estén).
- **Bug real encontrado al verificar**: en modo fusión, un Cliente ya existente nunca se renombra — pero si una fila de Empresas/Centros/Trabajadores del mismo archivo lo referenciaba por el nombre *nuevo* que traía el archivo (distinto al que ya tenía en base de datos), esa fila dejaba de encontrarlo en el momento de escribir (aunque el análisis previo sí lo había resuelto correctamente, porque en ese momento el nombre del archivo "cuenta" como válido). Corregido indexando los Clientes tanto por su nombre real en base de datos como por el nombre que trae el archivo, apuntando ambos al mismo registro.
- Verificado end-to-end en navegador con tres archivos de prueba (sin datos reales): (1) alta completa — 2 Clientes, 1 Empresa, 2 Centros, 2 Trabajadores creados; (2) reimportación en modo fusión con un Cliente "renombrado" en el archivo, un Centro con campos antes vacíos y un Trabajador con fecha de nacimiento/email antes vacíos — 0 clientes actualizados (correcto, el nombre no cambió en base de datos), 1 Centro y 1 Trabajador actualizados (solo los campos que estaban vacíos); (3) mismo Centro en modo "Reemplazar" — el código sí se sobrescribió esta vez. 57/57 pruebas automatizadas pasan.

## Fase 19 — Modo claro/oscuro guardado por usuario

✅ Completa. Último punto del backlog post-lanzamiento. La Fase 6 ya había preparado los tokens de color para `prefers-color-scheme: dark`, pero no existía ningún control para elegirlo explícitamente — solo seguía la preferencia del sistema operativo, y esa preferencia no viajaba con la cuenta del usuario entre dispositivos.

- **`ApplicationUser.Tema`** (nuevo, `TemaPreferido`: Sistema/Claro/Oscuro, por defecto Sistema — sin cambio de comportamiento para quien no toca el selector): se guarda en la cuenta, no en `localStorage`, para que el ajuste viaje con el usuario a cualquier dispositivo donde inicie sesión, igual que cualquier otro dato de su perfil.
- **`tokens.css`**: nuevos bloques `:root[data-theme='claro']` / `:root[data-theme='oscuro']`, con los mismos valores que ya existían en `@media (prefers-color-scheme: dark)` — un atributo en `:root` siempre gana a un `@media`, así que esto permite forzar un tema independientemente del sistema operativo sin tocar ningún componente (todos ya consumían las variables, nunca un color fijo).
- **`SelectorTema`** (nuevo componente, en la cabecera junto a "Cerrar sesión"): un `<select>` que aplica el tema vía interop de JS (`tema.js`, `document.documentElement.dataset.theme`) y lo guarda en `ApplicationUser` con `UserManager.UpdateAsync` — mismo patrón que ya usa `Usuarios.razor.cs` para leer/escribir Identity directamente desde Web, sin pasar por Application (Identity vive fuera del CQRS del dominio, ver `ARCHITECTURE.md`). Como el resto de componentes fuera de `@Body`, necesita `@rendermode="InteractiveServer"` explícito en `MainLayout.razor` (mismo gotcha ya documentado en Fase 2 con el buscador global).
- **Compromiso aceptado, documentado explícitamente**: el tema se aplica después de que el circuito de Blazor conecta, no en el HTML servido en frío — un usuario con Claro u Oscuro elegido explícitamente puede ver un parpadeo muy breve al tema del sistema en la primera carga de cada sesión. Evitarlo del todo exigiría leer la cookie de sesión desde el HTML servido (un mecanismo que ningún otro componente de la app usa todavía) — se dejó fuera por desproporcionado para esta fase.
- Verificado end-to-end en navegador: cambiar a "Oscuro" repinta toda la aplicación (sidebar, tarjetas, colores de semáforo) sin recargar; recargar la página por completo mantiene el tema Oscuro (confirma que persiste en la cuenta, no solo en memoria del circuito); volver a "Claro" repinta de vuelta correctamente. 57/57 pruebas automatizadas pasan.

Con esta fase se completa el backlog post-lanzamiento acordado con el usuario.

## Fase 20 — Credenciales de acceso de Empresa (URL/empresa/usuario/contraseña con copiar)

✅ Completa. Petición explícita del usuario, acotada tras una pregunta de aclaración: no reutiliza `PlataformaAcceso` (esa entidad ya existía, 1:1 con **Centro**, sin UI todavía) — es un concepto nuevo y separado, 1:1 con **Empresa**.

- **`CredencialAccesoEmpresa`** (nuevo, `CaeManager.Domain/Empresas/`): `UrlAcceso`, `CampoEmpresa` (el dato extra que piden algunas plataformas externas además de usuario/contraseña — según la plataforma se llama "Empresa", "Cliente" o "Proveedor", así que se modela como texto libre sin significado propio), `Usuario`, `Contrasena`. Mismo patrón de cifrado en reposo que `PlataformaAcceso` (Fase 0): un `ValueConverter` de EF Core con `IDataProtector`, con su propio "purpose" criptográfico separado (`CaeManager.CredencialAccesoEmpresa.Credenciales.v1`) para no compartir claves derivadas con las credenciales de Centro.
- **`GuardarCredencialAccesoEmpresaCommand`**: alta o edición en un único comando (upsert) — una Empresa tiene como mucho un juego de credenciales, así que no hace falta el patrón Crear/Editar separado del resto de módulos.
- **`AuditoriaInterceptor`**: `CredencialAccesoEmpresa` añadida al diccionario de tipos con campos sensibles enmascarados (`Usuario`/`Contrasena` → `"***"` en el historial) — confirmado que el registro de auditoría nunca guarda ninguno de los dos en texto plano.
- **`/empresas`**: al editar una Empresa (no al crear una — hace falta un Id antes de poder asociarle credenciales) aparece una sección "Credenciales de acceso a plataforma externa" con los 4 campos, la contraseña como `type="password"`, y un botón "Guardar credenciales" independiente del guardado del resto del formulario.
- **`BotonCopiar`** (nuevo componente reutilizable, `Components/DesignSystem/`) + `clipboard.js`: copia el valor de un campo al portapapeles vía `navigator.clipboard.writeText` (con reserva a `document.execCommand('copy')` si el contexto no es seguro) y confirma con un Toast. Deshabilitado cuando el campo está vacío. Uno junto a cada uno de los 4 campos.
- Verificado end-to-end en navegador: credenciales guardadas y recuperadas correctamente (round-trip cifrado/descifrado); en la base de datos, `UrlAcceso`/`CampoEmpresa` quedan en texto plano (no son sensibles) pero `Usuario`/`Contrasena` quedan cifrados de verdad (confirmado leyendo el archivo SQLite directamente); el botón "Copiar" del Usuario deja el valor correcto en el portapapeles; el registro de Auditoría de esta entidad tiene `Usuario`/`Contrasena` enmascarados como `"***"`, nunca el valor real. 57/57 pruebas automatizadas pasan.

## Fase 21 — Preparación para despliegue compartido (Railway)

✅ Completa. El usuario necesitaba que el equipo pudiera probar la app 100% desde el navegador, sin instalar .NET en cada máquina — petición de infraestructura, no de producto, pero con un bug real de fondo que había que corregir antes de exponer la app fuera de una máquina de desarrollo.

- **Bug real encontrado**: `services.AddDataProtection()` no persistía las claves de cifrado en ningún sitio estable — cada reinicio del proceso generaba unas nuevas, y todo lo cifrado con las anteriores (credenciales de Empresa/Centro, Fase 0 y Fase 20) dejaba de poder descifrarse silenciosamente. En una máquina de desarrollo que casi nunca se reinicia esto pasaba desapercibido; en un contenedor que se redespliega en cada push, habría roto las credenciales guardadas en el primer redeploy. Corregido con `PersistKeysToFileSystem` a una ruta configurable (`DataProtection:RutaClaves`), igual patrón que ya usaba `AlmacenamientoArchivos:Ruta` para los PDFs.
- **`IdentitySeeder`**: email/contraseña del administrador inicial ahora configurables (`AdministradorInicial:Email` / `AdministradorInicial:Contrasena`) — sin configurar nada siguen siendo los valores por defecto de siempre (no rompe nada en local), pero un despliegue compartido ya no tiene por qué arrancar con una contraseña pública en el propio código fuente.
- **`ForwardedHeaders`**: añadido antes del resto del pipeline — detrás de un proxy inverso (Railway y cualquier despliegue en contenedor) Kestrel solo ve tráfico HTTP interno; sin esto, `UseHttpsRedirection`/`UseHsts` no reconocen la petición original como HTTPS.
- **`/salud`**: endpoint sin autenticación para healthchecks del host.
- **`Dockerfile`** (multi-stage, SDK de build + runtime ASP.NET) y **`.dockerignore`**: probado end-to-end en local con Docker — build limpio, contenedor arrancado con `PORT` dinámico (el mecanismo que usa Railway), `/salud` responde 200, inicio de sesión funciona y el Dashboard carga correctamente dentro del contenedor.
- **`DEPLOY.md`** (nuevo): guía paso a paso de Railway — volumen persistente para la base de datos/PDFs/claves de cifrado, variables de entorno necesarias, y la advertencia explícita de mantener el servicio en una sola réplica (SQLite no soporta escritura concurrente desde varios procesos).

## Fase 22 — Corrección: interactividad rota en el despliegue de Railway

✅ Completa. Tras el primer despliegue real (Fase 21), ningún botón funcionaba (`+ Nuevo Cliente/Empresa/Centro`, etc.) — la consola del navegador mostraba `_framework/blazor.web.js` devolviendo 404.

- **Causa real, nada que ver con `Program.cs`**: `dotnet publish` sin fijar un `RuntimeIdentifier` deja que el SDK de la imagen `mcr.microsoft.com/dotnet/sdk:10.0` (banda `10.0.3xx`, más reciente que la usada en Fase 21 al validar en local) decida qué paquete de runtime restaurar. Con esa banda, el paquete que aporta los assets estáticos propios del framework (`_framework/blazor.web.js` y variantes) nunca se restaura — el publish termina sin ningún error, el archivo simplemente no se genera, y por tanto el circuit de SignalR jamás llega a establecerse en ningún botón/formulario interactivo de la app, autenticado o no. Confirmado reproduciendo el build exacto de Railway en local: `MapStaticAssets().AllowAnonymous()` (Fase 21) nunca fue el problema — ni siquiera aparecía como endpoint registrado para ese archivo dentro del contenedor.
- **Corrección, solo en `Dockerfile`**: publicar con `-r linux-x64 --self-contained false` (framework-dependent, pero forzando qué paquete de runtime se restaura) y sin `--no-restore` en el publish final — el restore hecho antes de copiar el código fuente (solo con los `.csproj`, para cachear capas) no basta para que el SDK detecte que necesita ese paquete; el restore completo, ya con los `.razor` presentes, sí lo detecta. `Program.cs` queda exactamente igual que en Fase 21.
- **Verificación**: reproducido el bug en local con el Dockerfile de Fase 21 (curl sin sesión a `_framework/blazor.web.js` devolvía 302→404 dentro del contenedor, igual que en Railway), corregido, y confirmado con Playwright de extremo a extremo dentro de un contenedor reconstruido: login → `blazor.web.js` 200 → `/_blazor/negotiate` 200 → clic en "+ Nuevo cliente" abre el drawer. Páginas protegidas (`/clientes` sin sesión) siguen redirigiendo a inicio de sesión, sin cambios de seguridad.

## Fase 23 — Módulo Subcontratas + base de datos genérica de pruebas

✅ Completa. Petición del usuario: un nivel adicional en la cadena de coordinación — la Subcontrata, contratada directamente por el Cliente, que presta servicio a una o varias Empresas con sus propios Trabajadores — más una base de datos genérica de volumen realista para hacer pruebas de carga básicas y verificar los cuatro perfiles con datos reales.

- **Domain**: `Subcontrata` (mismos datos que Empresa), `SubcontrataCliente` y `SubcontrataEmpresa` (muchos-a-muchos, mismo patrón que `EmpresaCliente`) y `CredencialAccesoSubcontrata` (mismo tratamiento que `CredencialAccesoEmpresa`, incluido el cifrado en reposo). `Trabajador` pasa a pertenecer a una Empresa **o** a una Subcontrata, nunca ambas — `EmpresaId`/`SubcontrataId` ahora nullable, con dos factories (`Trabajador.DeEmpresa`/`Trabajador.DeSubcontrata`) en vez de un constructor público, para que la invariante no se pueda saltar desde fuera del dominio.
- **Infrastructure**: configuraciones EF, repositorios y migración nuevos para las 4 entidades. `AuditoriaInterceptor` actualizado para redactar también Usuario/Contraseña de `CredencialAccesoSubcontrata` en el historial (el mismo tratamiento que ya tenía `CredencialAccesoEmpresa` — un descuido aquí habría dejado credenciales en texto plano en Auditoría).
- **Application**: CRUD completo de Subcontratas (crear/editar/eliminar/credenciales/listado/selector/detalle). `CrearTrabajador`/`ObtenerTrabajador*` adaptados al empleador polimórfico. `Dashboard` y `Reportes` — que antes hacían `INNER JOIN` directo con Empresa — pasan a `LEFT JOIN` con Empresa y Subcontrata y usan el nombre que corresponda: con el `INNER JOIN` original, cualquier Trabajador de Subcontrata habría desaparecido en silencio de "documentos que requieren atención", "empresas en riesgo" y del reporte completo de vigencia documental.
- **Web**: módulo `/subcontratas` (mismo patrón que Empresas: listado, alta/edición con dos listas de selección — Clientes que la contrataron y Empresas a las que presta servicio — y credenciales de acceso). `/trabajadores` añade un selector "Empresa o Subcontrata" al dar de alta. Buscador global (Ctrl/Cmd+K) incluye Subcontratas como categoría nueva.
- **`DatosPruebaSeeder`** (nuevo, apagado por defecto vía `DatosPrueba:Activo`): siembra 200 Clientes, 220 Empresas, 200 Subcontratas, ~300 Centros, 500 Trabajadores y ~1000 Documentos con vencimientos repartidos entre vencido/urgente/próximo/vigente, más 3 usuarios de prueba por cada uno de los 4 perfiles (contraseña común, documentado en `DEPLOY.md`) — solo siembra si la base de datos está vacía de Clientes, para no duplicar en cada redeploy.
- **Verificado**: 69 tests automatizados en verde, y de extremo a extremo en navegador con la base de datos de prueba recién sembrada — alta de Subcontrata con sus dos listas de asociación, alta de Trabajador de Subcontrata, Dashboard y Reportes con datos mixtos Empresa/Subcontrata sin errores, e inicio de sesión con un usuario de prueba de perfil Consulta confirmando que no ve secciones solo-Administrador.
- **Fuera de alcance de esta fase** (igual que se hizo con el CIF en su momento): la plantilla de importación masiva de Trabajadores por Excel (Fase 5) y la importación combinada (Fase 18) siguen dando de alta trabajadores solo bajo Empresa — no se extendieron a Subcontrata. Tampoco se tocaron los exportes de Reportes más allá de que ya no pierden filas (siguen etiquetando la columna como "Empresa", ahora con el nombre que corresponda dentro).

## Fase 24 — Menú lateral reorganizado por grupos funcionales

✅ Completa. Petición del usuario: agrupar los enlaces del menú lateral (hasta ahora una lista plana de 16 enlaces) en 4 bloques con encabezado, más Dashboard suelto arriba — Negocio (Clientes/Empresas/Subcontratas/Centros), Operación (Trabajadores/Asignaciones/Documentos), Control (Alertas/Calendario/Reportes) y Administración (Usuarios/Roles/Tipos de Documento/Configuración/Auditoría/Importar desde Excel, todo bajo el mismo `AuthorizeView` de rol Administrador que ya tenían).

- **`NavMenu.razor`**: añadido un `<span class="nav-grupo-titulo">` literal antes de cada bloque (no reutiliza `.nav-item`: no es un enlace). El bloque de Administración conserva su único `AuthorizeView` envolvente — ahora también envuelve el encabezado del grupo, para que un usuario sin rol Administrador no vea ni el título "Administración" ni ningún enlace del grupo (antes solo se ocultaban los enlaces, no había encabezado que ocultar).
- **`NavMenu.razor.css`**: nueva regla `.nav-grupo-titulo` (mayúsculas, tipografía pequeña, color apagado, `margin-top` para separar del grupo anterior, sin margen en el primero). No lleva `::deep` porque, a diferencia de `.nav-item` (que estiliza el `<a>` interno de `<NavLink>`, un componente hijo), el `<span>` está escrito directamente en el propio `NavMenu.razor` — mismo html, sin aislamiento que atravesar.
- Nombres de ruta y de enlace sin cambios (se mantiene "Subcontratas", coherente con el resto de la app — entidad `Subcontrata`, ruta `/subcontratas`, título de página — en vez de "Subcontratistas" tal como apareció en la petición, para no introducir una etiqueta distinta al resto del sistema).
- **Verificado en navegador con Playwright**: los 4 grupos aparecen en el orden pedido con sus encabezados, todos los enlaces navegan a la ruta correcta (probado Subcontratas→`/subcontratas` y Usuarios→`/usuarios`), sin errores de página. Durante la verificación se encontró y corrigió un problema no relacionado con este cambio (ver nota abajo) antes de poder confirmar el resultado visual.
- **Nota — bug de entorno encontrado durante la verificación (no relacionado con el menú)**: al lanzar la app ejecutando el `.dll` directamente sin `ASPNETCORE_ENVIRONMENT=Development`, ASP.NET Core no habilita "Static Web Assets" y el sistema de parcheo de rutas fingerprint en desarrollo intenta servir `blazor.web.js`, `CaeManager.Web.styles.css` y los `.razor.js` de componentes leyéndolos directamente de `wwwroot`, donde no existen (viven en el manifiesto de Static Web Assets) — resultado: 500 en esos tres recursos y `tokens.css` servido con 0 bytes, que a su vez dejaba sin dimensiones a los iconos SVG y los hacía renderizar a un tamaño intrínseco enorme (el layout entero, no solo el menú). No es un bug de código de este repo; es un requisito de cómo se lanza la app en local que no estaba documentado. Añadido `ASPNETCORE_ENVIRONMENT=Development` al comando de verificación local usado en esta sesión.

## Fase 25 — Pruebas intensivas end-to-end previas a despliegue en Railway

✅ Completa. Petición del usuario: "pruebas intensivas de todos los casos de uso, casillas y opciones para poder desplegar en railway y hacer pruebas de mi parte" — una pasada de QA amplia sobre toda la app, no solo el último cambio, antes de que el usuario la despliegue y la pruebe él mismo en Railway.

- **Método**: script de Playwright (no un test automatizado permanente del repo — vive en el scratchpad de la sesión) que recorre los 20 módulos/rutas de la app con datos de prueba sembrados (`DatosPrueba__Activo=true`, 200 clientes/220 empresas/200 subcontratas/264 centros/500 trabajadores/996 documentos + 12 usuarios de prueba, uno por rol) y ejerce en cada uno: carga de listado, búsqueda/filtros en vivo, alta con sus checkboxes/selectores propios, edición, baja/eliminación con diálogo de confirmación, y validaciones de campo (CIF inválido, DNI generado con dígito de control real).
- **Cobertura**: Login (credenciales inválidas/válidas) · Dashboard (KPIs) · Clientes (checkbox "Cliente crítico", validación de CIF) · Empresas (selector múltiple de Clientes, credenciales de acceso reveladas tras crear) · Subcontratas (dos selectores múltiples, checkbox "Solo mostrar relacionados") · Centros (selectores de Cliente/Empresa sin cascada) · Trabajadores (radio Empresa/Subcontrata, DNI con dígito de control) · Documentos (selector de Ámbito, subida real de un PDF) · Asignaciones · Alertas (clic abre gestión) · Calendario · Reportes (descarga real de Excel) · Usuarios (selector de Rol, activar/desactivar) · Roles · Tipos de Documento (cascada de filtros Cliente→Empresa→Centro, checkbox "Aplica vencimiento automático") · Configuración (umbrales) · Auditoría (filtro por entidad) · Importación (enlaces a plantillas) · buscador global Ctrl+K (navegación con flechas) · selector de tema (claro/oscuro) · control de acceso por rol (Consulta/Supervisor/EjecutivoCae/Administrador).
- **Resultado**: 73/73 verificaciones en verde en la corrida final, cero errores de página (`pageerror`), cero errores de consola y cero respuestas 5xx durante todo el recorrido. `dotnet build` sin errores y 90/90 pruebas automatizadas del repo en verde. Las primeras corridas del script encontraron 11 fallos que resultaron ser todos del propio script de prueba (botones "Guardar" ambiguos entre el formulario principal y "Guardar credenciales", condiciones de carrera esperando que Blazor Server terminara un round-trip antes de leer el DOM, un enlace confundido con un botón, un radio "Empresa/Subcontrata" no contemplado en Trabajadores) — ninguno era un bug real de la aplicación; quedaron corregidos en el script antes de la corrida limpia final.
- **Hallazgo real, pendiente de decisión** (no es un bug de código, es una pregunta de producto): los roles Consulta, Supervisor y EjecutivoCae pueden crear/editar/eliminar registros en Clientes, Empresas, Subcontratas, Centros, Trabajadores, Documentos y Asignaciones exactamente igual que Administrador — hoy **no existe ningún pipeline de autorización a nivel de Command/Query** (`grep` sin resultados para un `IPipelineBehavior` de autorización), y el único control de rol real en toda la app son los `[Authorize(Roles = Administrador)]` de página en Usuarios/Roles/Tipos de Documento/Configuración/Auditoría/Importación — confirmado también en el menú lateral, donde solo el grupo "Administración" se oculta para los otros 3 roles. Ni `PROJECT.md` ni `ARCHITECTURE.md` especifican qué debería poder hacer cada uno de los 4 roles más allá de nombrarlos ("alineados 1:1 con los cuatro dashboards del brief"), así que no se ha asumido ni implementado ninguna restricción — de momento los 4 roles son operativamente equivalentes salvo por la sección de Administración. Si el nombre "Consulta" implica que ese rol debe ser de solo lectura en el resto de la app, es una decisión de producto pendiente de confirmar antes de abrir el sistema a usuarios con ese rol en producción.

## Fase 26 — Bug de producción: límite de logs de Railway superado al sembrar datos de prueba

✅ Completa. Tras activar `DatosPrueba__Activo=true` en Railway (ver Fase 25/backlog #1), el arranque disparó el límite de Railway de 500 logs/seg y llegó a descartar más de 26.000 mensajes (`Railway rate limit of 500 logs/sec reached for replica`).

- **Causa**: `appsettings.json` tenía `Logging:LogLevel:Default` en `Information` sin excluir la categoría `Microsoft.EntityFrameworkCore.Database.Command`, que registra el texto completo y los parámetros de **cada sentencia SQL ejecutada**. El sembrado genérico de pruebas inserta miles de filas (200 clientes, 220 empresas, 200 subcontratas, 264 centros, 500 trabajadores, 996 documentos, más las tablas puente many-to-many) — cada INSERT genera varias líneas de log, disparando el límite de Railway casi de inmediato.
- **Arreglado**: añadida la entrada `"Microsoft.EntityFrameworkCore.Database.Command": "Warning"` en `Logging:LogLevel` de `appsettings.json`. Suprime el volcado de cada sentencia SQL sin perder los mensajes propios de la app (el resumen `Sembrando…`/`Datos de prueba sembrados: ...` sigue apareciendo, porque usa un logger de otra categoría).
- **Verificado en local**: mismo sembrado completo (200/220/200/264/500/996) generó **28 líneas de log en total** en vez de miles, sin ningún `Executed DbCommand`, y con el mensaje de resumen final intacto. `dotnet build` sin errores, 90/90 tests en verde.
- **Nota**: esta categoría de log es muy útil en local para depurar consultas lentas — sigue disponible ahí porque el entorno de desarrollo no toca `appsettings.json`; solo se apaga el ruido en el nivel `Default` que hereda producción. Si en el futuro hace falta depurar SQL en Railway puntualmente, se puede subir esta categoría a `Information` temporalmente vía variable de entorno (`Logging__LogLevel__Microsoft.EntityFrameworkCore.Database.Command=Information`) sin tocar código.

## Fase 27 — Dos bugs reales de producción encontrados probando en Railway: tema que no persiste y ripple que crece sin límite

✅ Completa. El usuario, probando ya sobre Railway, reportó dos comportamientos rotos.

**1. El tema (claro/oscuro) se reiniciaba al navegar entre secciones.** Causa: la navegación "enhanced" de Blazor (la que dispara cualquier clic en el menú lateral) vuelve a pedir al servidor el HTML de la página destino y hace un *morph* del DOM contra ese HTML nuevo — que nunca incluye `data-theme` en `<html>`, porque ese atributo solo lo pone `wwwroot/js/tema.js` en el cliente tras conectar el circuito. El morph lo borra en cada navegación, y como `SelectorTema.razor` vive en el layout persistente (no se vuelve a montar), su `OnAfterRenderAsync(firstRender: true)` — el único punto donde se reaplicaba — no vuelve a dispararse.
- **Arreglado**: `tema.js` ahora guarda el último tema aplicado en una variable de módulo y escucha el evento `Blazor.addEventListener('enhancedload', ...)` (el evento oficial de Blazor tras cada navegación enhanced) para reaplicarlo, sin depender de que ningún componente C# se vuelva a montar.
- **Verificado en navegador con Playwright**: tema puesto en "Oscuro", navegado por 5 secciones distintas (Clientes, Empresas, Trabajadores, Dashboard, Documentos) — `data-theme="oscuro"` se mantiene en las 5.

**2. El ripple de `.boton` se acumulaba sin límite y el botón parecía "crecer".** Causa real, distinta de lo que parecía a simple vista: `.boton-ondulacion` estaba definido en `Boton.razor.css`, un archivo de CSS **aislado** de Blazor — el compilador reescribe todo selector ahí a la forma `.clase[b-xxxxxxxx]` (el mismo atributo de scope que lleva el `<button>`). El `<span class="boton-ondulacion">` lo crea JS puro (`document.createElement`, ver `wwwroot/js/microinteracciones.js`) para no pagar JS interop por instancia — y un elemento creado fuera del árbol de Razor **nunca recibe ese atributo de scope**, así que la regla aislada nunca lo alcanzaba: sin `position: absolute` (quedaba en `position: static` por defecto) el span caía en flujo normal dentro del `inline-flex` del botón, con el ancho/alto grande que le fija el JS inline — inflando el botón en cada clic. Y como la regla con `animation:` tampoco lo alcanzaba, la animación nunca llegaba a aplicarse de verdad, `animationend` nunca disparaba, y el span nunca se quitaba — de ahí la acumulación.
- **Arreglado**: movido `.boton-ondulacion` + `@keyframes expandir-ondulacion` + su bloque de `prefers-reduced-motion` de `Boton.razor.css` a `wwwroot/css/base.css` (global, sin aislar) — mismo patrón ya usado para el revelado escalonado (`list-page.css`), que tiene el mismo problema de fondo (clases aplicadas a filas de `QuickGrid`, fuera del árbol de Razor del componente que las estilizaba). Además, `microinteracciones.js` ahora omite crear el span si `prefers-reduced-motion: reduce` está activo (coherente con el CSS) y añade un `setTimeout` de 650 ms como red de seguridad por si `animationend` no dispara por cualquier otro motivo (pestaña en segundo plano, etc.) — no depende ya de un único mecanismo de limpieza.
- **Verificado en navegador con Playwright**: 6 ciclos de abrir/cancelar el formulario de Empresas — antes dejaban spans creciendo sin límite (confirmado en un script de reproducción: 251px → 614px → 1286px → 1842px → 2622px de ancho); ahora quedan **0 spans** tras los 6 ciclos, y el ancho del botón "+ Nueva empresa" es idéntico antes y después (179.5625px ambas veces).

`dotnet build` sin errores, 90/90 tests en verde.

## Fase 28 — Módulo Visitas: programar y verificar accesos antes de que ocurran

✅ Completa. Petición del usuario: cuando el cliente comunica que uno o varios Trabajadores deben entrar a un Centro durante un rango de fechas (p. ej. "el trabajador X debe ingresar a Coca Cola, Centro Y, del día A al día B"), el sistema debe verificar automáticamente si la documentación de la Empresa y de los Trabajadores implicados está vigente, mostrar el número de visitas programadas en el Dashboard, verlas en el Calendario, conservar el historial una vez pasada la fecha de fin, y permitir marcar "Notificado al cliente" para llevar el control de gestión.

- **Domain** (`CaeManager.Domain/Visitas/`): `Visita` (`EntidadBase` — soft delete estándar, igual que Cliente/Empresa/Centro) con `CentroId`, `FechaInicio`/`FechaFin`, `NotificadoCliente`, `Notas` y `EstaActiva(hoy)` (`FechaFin >= hoy`). `VisitaTrabajador` es la asociación muchos-a-muchos con Trabajador (mismo patrón que `SubcontrataCliente`: sin ciclo de vida propio, desvincular es baja física). Una Visita admite varios Trabajadores a la vez (un equipo que entra junto), no solo uno — el ejemplo del usuario usaba singular pero la propia petición habla de "los empleados" en plural al describir la verificación.
- **Verificación automática de documentación** (`ObtenerVisitasQuery`): reutiliza `CalculadoraEstadoDocumento` — la misma función que Dashboard/Alertas/Calendario — sobre los Documentos existentes de la Empresa del Centro (Ámbito Empresa) y de cada Trabajador de la visita (Ámbito Trabajador). "Documentación completa" exige que la Empresa y todos los Trabajadores tengan **al menos un** Documento y que todos estén Vigente o NoAplica — cero Documentos cuenta como pendiente de gestionar, igual que uno vencido, porque significa que nadie ha subido nada todavía. No se implementó una reconciliación contra qué tipos de documento son obligatorios por Centro (la entidad `RequisitoDocumental` existe en el dominio pero nunca se llegó a conectar a ningún flujo — es texto libre, no structured por tipo — así que no hay de dónde sacar esa lista todavía); queda fuera de alcance de esta ronda.
- **Aparecer/desaparecer**: la lista activa (`SoloActivas=true`, por defecto) filtra `FechaFin >= hoy` — al pasar la fecha de fin, la visita deja de aparecer ahí automáticamente sin ningún job en segundo plano (es un filtro de fecha, no un estado que alguien tenga que cambiar), pero sigue existiendo y es consultable desde el propio filtro (desmarcando "Solo activas") o desde Auditoría si se elimina — igual que Asignacion con FechaBaja.
- **Web** (`/visitas`, grupo "Operación" del menú, icono nuevo — puerta con flecha entrando): listado con QuickGrid (Centro, Cliente/Empresa, Fechas, nº Trabajadores, badge de Documentación Completa/Por gestionar, checkbox "Notificado al cliente" editable directamente desde la fila sin abrir el formulario, Editar/Eliminar). Alta/edición en drawer: selector de Centro (solo en alta, igual que Cliente/Empresa en Centros — no se puede reasignar después), fechas de inicio/fin, `SelectorMultiple` de Trabajadores (mismo componente que Empresas→Clientes y Subcontratas→Clientes/Empresas), checkbox Notificado, notas.
- **Dashboard**: séptima tarjeta KPI "Visitas programadas" (cuenta `FechaFin >= hoy`), añadida a `ObtenerKpisDashboardQuery` sin romper las seis existentes — el grid es `auto-fit`, así que reflow es automático.
- **Calendario**: `ObtenerVisitasParaCalendarioQuery` — a diferencia de `ObtenerVencimientosMesQuery` (un vencimiento cae en un día exacto), aquí el filtro es de solapamiento de rango (`FechaInicio <= últimoDíaDelMes && FechaFin >= primerDíaDelMes`). Cada día del rango de una Visita muestra un segundo indicador (Badge `Info`, color secundario teal) junto al de vencimientos, sin mezclarlos — al hacer clic en el día, el modal ahora tiene dos secciones ("Vencimientos" y "Visitas programadas") en vez de solo una.
- **Verificado en navegador con Playwright** (15/15 comprobaciones): KPI del Dashboard aparece y sube de 0 a 1 al crear una visita; selector de Centro carga las 264 opciones sembradas; SelectorMultiple de Trabajadores funciona; documentación calculada correctamente como "Por gestionar" con datos de prueba reales; checkbox "Notificado al cliente" se marca desde la fila y persiste; indicador y sección de visitas aparecen en el Calendario del día correspondiente; editar mantiene el Centro de solo lectura; eliminar pide confirmación y la visita desaparece de la lista activa (soft delete). Sin errores de página. `dotnet build` sin errores, 104/104 tests en verde (añadidos `VisitaTests`/`VisitaTrabajadorTests`, mismo patrón que `SubcontrataTests`).
- **Migración EF**: `AgregarVisitas` — dos tablas nuevas (`Visitas`, `VisitasTrabajadores`), ninguna alteración sobre tablas existentes.

## Fase 29 — Catálogo de documentos obligatorios de Empresa + módulo Vehículos

✅ Completa. Petición del usuario: (1) sembrar los 13 documentos base que ProjectHydra exige a **todas** las empresas cliente (Seguridad Social, Hacienda, ITA, RLC/TC1 y sus variantes, RNT/TC2, Mutua, Seguro de Responsabilidad Civil, SPA, EVR, PAP y Tarjeta CIF como único opcional), y (2) una sección nueva de Vehículos (Nombre, Modelo, Número de placa) con su propia documentación (ITC, Ficha Técnica, Seguro, Autorización de circulación).

- **`EsObligatorio` en `TipoDocumento`** (`CaeManager.Domain/Documentos/TipoDocumento.cs`): propiedad nueva (`bool`, por defecto `false`), fijada en el constructor y editable en `Actualizar`. Es el concepto que faltaba desde la Fase 28 — ahí se documentó explícitamente que `RequisitoDocumental` nunca llegó a conectarse a ningún flujo porque es texto libre, no estructurado por tipo. `EsObligatorio` resuelve esto de forma mínima: no reemplaza `RequisitoDocumental` (que sigue sin usarse, fuera de alcance) ni introduce una relación Tipo↔Centro nueva, solo marca qué Tipos de Documento de ámbito Empresa son innegociables para cualquier cliente.
- **Catálogo sembrado** (`TipoDocumentoSeedData.cs`): 13 filas nuevas de ámbito Empresa (GUIDs fijos `20000000-...`) y 4 de ámbito Vehículo (GUIDs fijos `30000000-...`), sumando 32 filas totales (antes 15). Decisiones tomadas sin preguntar, documentadas aquí por si hay que revisarlas: RLC/TC1 y sus tres variantes (Recibo de pago, combinado, RNT/TC2) llevan `VigenciaMeses=3` porque el usuario explicó que el documento de un periodo (p. ej. 01/05) no se emite hasta 2 meses después y vence el mes siguiente a eso — 3 meses desde la fecha de emisión real cubre ese desfase. Los tipos con vigencia "variable" o "sin especificar" (Hacienda, Mutua, Seguro RC, SPA, EVR, PAP) quedan con `AplicaVencimientoAutomatico=false` — la fecha de vencimiento se introduce a mano al subir cada documento, igual que ya funcionaba para otros tipos manuales del catálogo original. Tarjeta CIF es el único con `EsObligatorio=false`, tal como el usuario lo marcó explícitamente como "(Opcional)".
- **Módulo Vehículos** (dominio, infraestructura, aplicación y web completos, mismo patrón end-to-end que Trabajador): `Vehiculo` (`CaeManager.Domain/Vehiculos/`) es `EntidadBase` con `EmpresaId?`/`SubcontrataId?` mutuamente excluyentes (constructor privado + `DeEmpresa`/`DeSubcontrata`, idéntico a `Trabajador`), `Nombre`/`Modelo`/`NumeroPlaca` (la matrícula se normaliza a mayúsculas y es única). CRUD completo en `/vehiculos` (grupo "Operación" del menú, icono nuevo) calcado de `Trabajadores.razor`: radio Empresa/Subcontrata, QuickGrid paginado con buscador y filtros, Editar/Eliminar con confirmación. `CrearVehiculoCommand`/`EditarVehiculoCommand` bloquean matrícula duplicada; `ObtenerVehiculosQuery` resuelve el nombre del empleador con el mismo doble `LEFT JOIN` que `ObtenerTrabajadoresQuery`.
- **`Documento.DeVehiculo`**: cuarta factory estática junto a `DeTrabajador`/`DeCliente`/`DeEmpresa` — `AmbitoAplicacion` gana el valor `Vehiculo` y `Documento` gana `VehiculoId?` (cuatro FKs opcionales mutuamente excluyentes en total). `/documentos` añade "Vehículo" como cuarta opción en el filtro de Ámbito y en "¿A quién pertenece?" del alta, con su propio selector (`ObtenerVehiculosParaSelectorQuery`) igual que Trabajador/Cliente/Empresa. `/tipos-documento` añade "Vehículo" al selector de Ámbito, una casilla "Obligatorio para todos los clientes" en el formulario y una columna con badge (Obligatorio en ámbar / Opcional en gris) en el listado.
- **`ObtenerVisitasQuery` actualizada**: el cálculo de "documentación completa" de la Empresa (Fase 28) pasa de la heurística antigua ("al menos un Documento y todos vigentes") a exigir, para **cada** `TipoDocumento` obligatorio de ámbito Empresa, al menos un Documento de ese tipo concreto en estado Vigente o NoAplica — ahora que existe una lista real de qué es obligatorio, en vez de "cualquier cosa que se haya subido". El criterio de Trabajador no cambia (`EsObligatorio` todavía no aplica a ese ámbito). No se conectó `Vehiculo` a `Visita` (no se creó `VisitaVehiculo`) — el usuario no lo pidió explícitamente y añadirlo habría exigido una decisión de producto propia (¿una visita de un vehículo es sobre su documentación, o sobre quién lo conduce?); queda como posible ampliación futura, no implementada.
- **Migración EF**: `AgregarVehiculosYEsObligatorio` — columna `EsObligatorio` en `TiposDocumento` (con backfill `false` a los 15 tipos semilla existentes), columna `VehiculoId` en `Documentos`, tabla nueva `Vehiculos`, e inserción de las 17 filas nuevas del catálogo.
- **Verificado en navegador con Playwright** (15/15 comprobaciones): alta de Vehículo con Empresa real, validación de matrícula duplicada mostrando el error sin cerrar el drawer, edición reflejada en la lista; catálogo de Tipos de Documento muestra las 32 filas con los badges Obligatorio/Opcional correctos (RLC/TC1 → Obligatorio, Tarjeta CIF → Opcional); selector de Ámbito=Vehículo en Documentos carga el vehículo recién creado y los 4 tipos de documento de Vehículo; Documento de Vehículo (ITC) se crea end-to-end y aparece filtrando por ese ámbito; Visitas sigue funcionando sin errores tras el cambio de criterio de documentación completa (probado creando una visita real, sin excepciones en consola ni en el servidor). `dotnet build` sin errores, 117/117 tests en verde (106 Domain + 6 Application + 5 Integration — añadidos `VehiculoTests`, casos nuevos en `DocumentoTests`/`TipoDocumentoTests`, y `MigracionesTests` actualizado de 15 a 32 tipos de documento sembrados).

## Fase 30 — Dashboard navegable: KPIs y filas de los boxes llevan a la pantalla filtrada

✅ Completa. Petición del usuario: las tarjetas KPI del Dashboard y las filas de los tres "boxes" de desglose deben ser seleccionables y llevar a la pantalla correspondiente con el filtro ya aplicado (vencidos, centros, trabajadores...), en vez de ser solo texto informativo.

- **`TarjetaMetrica`**: nuevo parámetro opcional `Enlace` — si se informa, la tarjeta se renderiza como `<a>` (con hover de elevación sutil, `prefers-reduced-motion` respetado) en vez de `<div>`; si no, se comporta exactamente igual que antes. Backward-compatible a propósito: los otros usos del componente (resúmenes de importación en Clientes/Documentos/Importación) no pasan `Enlace` y siguen sin ser clicables, porque ahí no tiene sentido navegar a ningún sitio.
- **Las 7 tarjetas KPI enlazan así**: Trabajadores activos → `/trabajadores`, Centros/plataformas → `/centros`, Documentos vencidos → `/alertas?estado=Vencido`, Urgentes → `/alertas?estado=Urgente`, Próximos → `/alertas?estado=Proximo`, Vigentes → `/documentos?estado=Vigente` (Alertas nunca incluye Vigente, por eso esta va a Documentos), Visitas programadas → `/visitas`.
- **Filtro de Estado nuevo en `/alertas` y `/documentos`**, ambos con soporte de query string (`?estado=`) para el deep-link desde el Dashboard:
  - `/alertas` ganó un selector "Estado" (Vencido/Urgente/Próximo/Todos) — antes no tenía ningún filtro, mostraba siempre la lista completa. Filtrado en memoria sobre la lista ya calculada (no hay repaginación en servidor, `ObtenerAlertasQuery` no cambió).
  - `/documentos` ganó un selector "Estado" (los 5 valores de `EstadoDocumento`, incluido Vigente y NoAplica). Esto obligó a tocar `ObtenerDocumentosQuery`: como el Estado se calcula siempre en memoria (nunca en SQL, ver comentario de la clase — es el principio establecido desde el Dashboard original), filtrar por Estado significa que la paginación también tiene que pasar a hacerse en memoria después de calcularlo, en vez de con `Skip`/`Take` en SQL como antes. Se cambió a traer el conjunto ya filtrado por ámbito/búsqueda completo, calcular Estado, filtrar y paginar en memoria — viable sin problema al volumen actual de Documentos de esta app (miles, no millones).
- **Filas de los tres boxes de desglose son clicables** (`Dashboard.razor.cs`, clase `fila-clicable` — solo añade `cursor: pointer`, el fondo de hover ya lo daba la regla global de `.tabla-datos tr:hover td`): "Documentos que requieren atención" lleva a `/documentos?documentoId={id}` (mismo patrón ya usado por Alertas, abre el drawer de renovación directamente); "Centros con más riesgo" y "Empresas con más riesgo" no tienen pantalla de detalle propia, así que llevan a `/centros?q={nombre}`/`/empresas?q={razónSocial}` reutilizando el prefiltro de búsqueda por query string que ya soportaban esas listas (mismo mecanismo `[SupplyParameterFromQuery(Name = "q")]` usado en Trabajadores/Vehículos).
- **Verificado en navegador con Playwright** (18/18 comprobaciones): las 7 tarjetas confirmadas como `<a>` clicables con la URL de destino correcta; Alertas y Documentos llegan con el selector de Estado ya preseleccionado según el query string, y las filas mostradas corresponden todas al estado filtrado; las tres filas de desglose navegan a su destino y, en Centros/Empresas, la lista de llegada queda filtrada al nombre correcto. `dotnet build` sin errores, 117/117 tests en verde (sin tests nuevos — el cambio es de Web/Application sin lógica de dominio nueva).

## Fase 31 — Jerarquía de roles y alcance de datos por cartera

✅ Completa (fundación). Petición del usuario, la más grande de la sesión hasta ahora: unificar Centros+Asignaciones en un árbol desplegable, evitar duplicados de Empresa/Subcontrata al vincularlas a un Cliente, hacer las pestañas del menú editables por Administrador, resolver la pregunta pendiente de Fase 3 sobre permisos por rol, añadir una pantalla "Supervisión" para Coordinador/Dirección CAE con comparativa y reasignación de cartera, un rol nuevo "Dirección CAE" por encima de Coordinador (antes "Supervisor"), que Gestor CAE (antes "EjecutivoCae") solo vea su propia cartera con su árbol de correlación completo, que Consulta sea 100% de solo lectura, y un rol nuevo "Cliente" para que el propio cliente vea (sin editar) su información. Dado el tamaño, se acordó con el usuario construir primero la base de la que depende todo lo demás — roles + motor de alcance de datos — y dejar el resto (fusión de pantallas, pantalla Supervisión, búsqueda anti-duplicados, menú editable, widget de notificaciones) para fases siguientes, documentadas más abajo como pendientes explícitos.

- **Decisiones tomadas con el usuario antes de tocar código** (`AskUserQuestion`, por el coste real de equivocarse en un modelo de autorización): (1) cuando una Empresa/Subcontrata sirve a Clientes de distintos Gestores CAE, cada Gestor solo ve en sus listados los Trabajadores/Documentos atados a sus propios Centros — pero el selector para *añadir* un Trabajador a uno de sus Centros sigue mostrando el catálogo completo de esa Empresa/Subcontrata, porque un mismo Trabajador puede prestar servicio a varios clientes a la vez y hace falta poder reasignarlo; (2) un usuario con rol Cliente se vincula a exactamente un Cliente de negocio, por CIF; (3) Dirección CAE ve todo el negocio igual que Administrador, pero no las pantallas de configuración del sistema.
- **Roles renombrados y ampliados** (`Roles.cs`): "Supervisor" → `CoordinadorCae` ("Coordinador CAE"), "EjecutivoCae" → `GestorCae` ("Gestor CAE") — mismo código de rol internamente estable (el `Name` real en `AspNetRoles`), solo cambia la etiqueta visible, vía un nuevo `Roles.NombreVisible(codigo)` que sustituye mostrar el código crudo en toda la UI (Usuarios, Roles). Roles nuevos: `DireccionCae` (por encima de Coordinador) y `Cliente`. La migración `AgregarRolesJerarquia` hace un `UPDATE` sobre las dos filas de rol existentes (conserva su Id, así que ningún `AspNetUserRoles` pierde el vínculo) e inserta las dos filas nuevas.
- **Modelo de cartera** (nuevo, mínimo): `Cliente.EjecutivoUsuarioId` (Guid? suelto, sin navegación — mismo patrón que `EliminadoPorUsuarioId`) es el Gestor CAE dueño de ese Cliente; se asigna solo automáticamente al crearlo si quien lo crea es un Gestor CAE (`CrearClienteCommand`, vía la nueva `ICurrentUserService.ObtenerRolActualAsync()`). `ApplicationUser.CoordinadorUsuarioId` (a qué Coordinador reporta un Gestor) y `ApplicationUser.ClienteId` (a qué Cliente representa un usuario Cliente, vinculado por CIF desde `Usuarios.razor` con una búsqueda en vivo nueva — `BuscarClientePorCifQuery`) se gestionan por ahora desde `/usuarios`; la reasignación en bloque queda para la pantalla Supervisión (pendiente).
- **`IAlcanceDatosService`** (interfaz en Application, implementación en Infrastructure porque necesita leer `ApplicationUser`, que Application no puede referenciar): resuelve, a partir del rol y usuario actuales, los Ids de Cliente/Centro/Empresa/Subcontrata/Trabajador/Vehículo visibles — `null` significa sin restricción (Administrador, DireccionCae, Consulta), una lista vacía significa "cartera sin asignar todavía" (nunca se confunde con "sin restricción"). Todo se deriva de relaciones ya existentes (`Centro.ClienteId`, `EmpresaCliente`, `SubcontrataCliente`, `SubcontrataEmpresa`, `Asignacion`) — no hace falta duplicar el dueño en cada tabla: reasignar una cartera es literalmente cambiar `Cliente.EjecutivoUsuarioId`.
- **Retrofit de alcance en el lado de lectura**: `ObtenerClientesQuery`/`ParaSelector`, `ObtenerCentrosQuery`/`ParaSelector`, `ObtenerEmpresasQuery`, `ObtenerSubcontratasQuery`, `ObtenerTrabajadoresQuery`, `ObtenerDocumentosQuery` (las 4 sub-consultas por ámbito), `ObtenerAsignacionesQuery`, `ObtenerVisitasQuery`/`ParaCalendario`, `ObtenerVencimientosMesQuery`, `ObtenerVehiculosQuery`, `ObtenerKpisDashboardQuery`, `ObtenerDesgloseDashboardQuery`, `ObtenerAlertasQuery`. Los selectores de "catálogo general para reutilizar" (Empresa, Subcontrata, Trabajador, Vehículo) se dejan **sin** restringir a propósito — es la mecánica que ya evita duplicados (Fase 5 del backlog) y la que permite añadir a un Trabajador compartido a un Centro propio.
- **Bloqueo de escritura para Consulta y Cliente**: `AutorizacionEscrituraBehavior`, nuevo `IPipelineBehavior` de MediatR registrado antes que `ValidationBehavior` — bloquea cualquier Command (distinguido por convención de nombre, no por interfaz marcador) para esos dos roles con un único mensaje de error, sin tener que tocar los ~40 Command existentes uno por uno.
- **Dashboard**: la escalera de visibilidad de las tres cajas de desglose (Fase 3) pasa de Administrador/Supervisor/EjecutivoCae a Administrador+DireccionCae+Consulta (empresas en riesgo) / +CoordinadorCae (centros en riesgo) / +GestorCae (documentos que requieren atención).
- **Menú lateral**: el rol Cliente tiene un menú deliberadamente distinto y mínimo (Dashboard, Empresas, Subcontratas, Centros, Trabajadores, Documentos — sin Asignaciones/Vehículos/Visitas/Alertas/Calendario/Reportes por ahora); el resto de roles de negocio conservan el menú completo de siempre. `/usuarios` pasa a ser accesible también para DireccionCae (antes solo Administrador), porque es donde se asigna Coordinador↔Gestor.
- **Aclaración recibida del usuario sobre Vehículos y Visitas**: un vehículo puede entrar en una Visita, pero normalmente no se informa de antemano cuál — solo importa que todos los vehículos de una Empresa/Subcontrata estén con su documentación al día. Decisión: no se conecta `Vehiculo` a `Visita` (confirma la decisión ya tomada sin preguntar en la Fase 29).
- **Verificado en navegador con Playwright** (22/22 comprobaciones): Roles y Usuarios muestran las etiquetas nuevas; Administrador/Consulta/DireccionCae ven los 200 Clientes sembrados; un Gestor CAE de prueba con 10 Clientes en cartera (de 30 repartidos entre los 3 Gestores de prueba) ve exactamente esos 10 y ninguno más; el Coordinador CAE de prueba con los 3 Gestores asignados ve la cartera combinada (30), y uno sin Gestores asignados ve 0; Consulta puede navegar y ver todo pero recibe "Tu rol no permite crear, editar ni eliminar datos" al intentar crear un Cliente, sin que el Command llegue a ejecutarse; el rol Cliente ve un menú acotado y sus propias Empresas (2, las de su Cliente vinculado) sin errores; Dashboard muestra la caja correcta según rol en cada nivel; DireccionCae entra a `/usuarios` pero es redirigido a login al intentar `/configuracion`. `dotnet build` sin errores, 131/131 tests en verde (añadidos: 4 en `ClienteTests` para `EjecutivoUsuarioId`/`AsignarEjecutivo`, 2 en `CrearClienteCommandHandlerTests` para la auto-asignación, 6 en `AutorizacionEscrituraBehaviorTests`, y `MigracionesTests` actualizado de 4 a 6 roles sembrados).

**Pendiente explícito, no implementado en esta fase** (dependía de esta base, ahora puede construirse):
1. **Fusión Centros + Asignaciones** en una sola pantalla con árbol desplegable por Centro mostrando sus Trabajadores asignados etiquetados "Empresa"/"Subcontrata". ([Issue #4](https://github.com/christopherjp1-jpg/Project-Hydra/issues/4))
2. **Búsqueda anti-duplicados al crear Subcontrata**: añadir CIF opcional a `Subcontrata` (hoy solo tiene `RazonSocial`, a diferencia de Empresa/Cliente) y un flujo de "busca antes de crear" en el alta. ([Issue #5](https://github.com/christopherjp1-jpg/Project-Hydra/issues/5))
3. **Menú editable por Administrador**: hoy la visibilidad de cada pestaña por rol es fija en `NavMenu.razor` (aunque ya correcta por rol) — falta la pantalla de configuración que permita a Administrador cambiarla sin tocar código, más la verificación en tiempo de ejecución de cada página (hoy el único candado real es el `[Authorize(Roles=...)]` de cada página). ([Issue #6](https://github.com/christopherjp1-jpg/Project-Hydra/issues/6))
4. **Pantalla "Supervisión"** (Coordinador CAE y Dirección CAE, dentro de Configuración): comparativa de cumplimiento/rendimiento entre los Gestores CAE de la cartera, y reasignación de Clientes (y todo lo asociado, que se resuelve solo al cambiar `EjecutivoUsuarioId`) entre Gestores para cubrir bajas o repartos de cartera. También la UI para que Dirección CAE/Administrador asignen Gestores a un Coordinador (hoy solo existe el campo en `/usuarios`, sin una vista de gestión en bloque). ([Issue #7](https://github.com/christopherjp1-jpg/Project-Hydra/issues/7))
5. **Widget de notificaciones diarias**, ahora ya definido por el usuario: documentos por vencer, y alerta si hay una Visita con algún Trabajador con documentación vencida — por usuario, visible solo para Gestor CAE. Sigue sin implementar (depende de decidir dónde vive en la UI — Dashboard vs. panel propio — no confirmado todavía).

## Fase 32 — Documentos: conversión de Word (.docx) a PDF con LibreOffice

✅ Completa (2026-07-18). Cierra la limitación dejada explícita en la Fase 16 ("no Word/Excel, que exigiría LibreOffice instalado en el servidor — fuera de alcance"). Esta funcionalidad se había implementado por completo en una sesión anterior sobre Geseme (rama `claude/file-conversion-pdf-ho4991`), pero esa rama nunca se fusionó al `main` de Geseme y por tanto tampoco llegó a Project-Hydra en la sincronización de la Fase 27 — detectado en una auditoría de ramas huérfanas del 2026-07-18 (ver también `ADR-002-single-tenant.md` y el fix IDOR del Issue #18, encontrados en la misma auditoría). Se adaptó a la estructura actual de `Documentos.razor`/`Documentos.razor.cs` (que ya no existía tal cual en el momento de la rama original) en vez de aplicar el commit tal cual.

- **`IConversorWordPdfService`** (`CaeManager.Application/Common/`): contrato con un único método, `ConvertirAPdfAsync(byte[] docx)`, mismo patrón que `IFileStorageService`.
- **`LibreOfficeConversorWordPdfService`** (`CaeManager.Infrastructure/Conversion/`): invoca `soffice --headless --convert-to pdf` como proceso externo, con perfil de usuario aislado por conversión (`-env:UserInstallation`) y timeout configurable (`ConversionWordPdf:TimeoutSegundos`, 60s por defecto). Se descartaron Aspose.Words/GroupDocs por licencia comercial y una API cloud de conversión por sacar documentos de trabajadores (dato PRL sensible) fuera del servidor — mismo criterio que PDFsharp/ClosedXML frente a QuestPDF.
- **Serialización de conversiones**: además del perfil aislado, las conversiones se serializan con un semáforo de instancia única — LibreOffice headless tiene fragilidad documentada con invocaciones concurrentes incluso con perfiles separados. Para el volumen esperado (uso interno, no SaaS) esto es preferible a perseguir un listener persistente tipo unoconv.
- **`ConversorArchivosPdf`** (`CaeManager.Web/Documentos/`): `Unificar` pasa a `UnificarAsync`, detecta `.docx` (`EsWord`) igual que ya detectaba imágenes.
- **`Dockerfile`**: instala el paquete `libreoffice-writer` (no `libreoffice` completo) en la imagen final de despliegue.
- **Verificado**: conversión real de un `.docx` a PDF con `soffice` real (no mockeado) en `tests/CaeManager.IntegrationTests/Conversion/`, incluidas dos conversiones concurrentes sin interferirse. `dotnet build`/`format`/148 tests en verde (109 Domain + 19 Application + 13 IntegrationTests + 7 Web.Tests).
- **Fuera de alcance**: Excel (`.xlsx`) sigue sin poder adjuntarse como Documento — no hay caso de uso reportado. Tampoco se añadió un test E2E de Playwright específico para la subida de un `.docx` — cubierto a nivel de integración (conversión real) y de UI ya existente (`FlujoCriticoTests` prueba la subida en general).

## Fase 33 — Login corporativo con Microsoft (SSO/Entra ID)

✅ Completa (2026-07-18). Petición del usuario: todo el mundo en la empresa ya tiene su cuenta de Microsoft interna (Entra ID), así que se añade como método de login adicional y se usa como capa extra de control específicamente para los roles editores — deja de bastar con una contraseña local para poder editar datos.

**Actualización (Fase 34, mismo día)**: el punto "nunca auto-provisiona cuentas / rechaza el login" de esta fase quedó **sustituido** — ver Fase 34 para el comportamiento vigente (auto-provisión sin rol + sala de espera). El resto de esta fase (opciones, restricción de rol para login local, botón condicional) sigue vigente sin cambios.

- **`AzureAdOptions`** (`CaeManager.Infrastructure/Identity/`): `TenantId`/`ClientId`/`ClientSecret` — sin las tres, el proveedor externo ni se registra en `Program.cs` (mismo patrón "inerte por defecto" que Sentry/Backups/Anthropic). Registrado como OpenID Connect single-tenant (`Authority = https://login.microsoftonline.com/{TenantId}/v2.0`) — restringe el login a la cuenta de Entra ID de la empresa, nunca "cualquier cuenta Microsoft".
- **Flujo de login** (`IdentityEndpointsExtensions`): `/cuenta/iniciar-sesion-microsoft` reta al esquema OIDC; `/cuenta/microsoft-callback` recibe la vuelta con la cookie externa temporal ya firmada por el middleware. Tampoco exige `DebeCambiarContrasena` (ese flujo es solo para la contraseña local).
- **`RestriccionLoginLocalClaimsTransformation`** (`IClaimsTransformation` global): mientras Entra ID esté configurado, cualquier sesión sin la marca de login por Microsoft queda con el rol efectivo limitado a `Consulta`, sin importar el rol real en base de datos — el login local no se bloquea, pero deja de dar permisos de edición. Se desactiva sola (sin cambios de código) si se quitan las variables de Railway, así que nunca bloquea el arranque en frío antes de tener SSO configurado y probado.
- **`Login.razor`**: botón "Iniciar sesión con Microsoft" condicional a `AzureAdOptions.EstaConfigurado`, con aviso explícito de que el login local queda en solo consulta.
- **Verificado en navegador real** (no solo tests): con Entra ID sin configurar, el botón no aparece y el login local se comporta exactamente igual que antes de esta fase (sin regresión). Con Entra ID configurado (credenciales de prueba, sin tenant real — no se pudo probar el intercambio real con Microsoft, ver limitación abajo), el botón aparece, el login local de un Administrador de prueba deja de poder entrar a `/usuarios`, y sigue autenticado (no fue deslogueado, solo perdió privilegios).
- **Limitación conocida, no verificable en este entorno**: el intercambio real de código OIDC contra un tenant de Entra ID real no se pudo probar (requiere un App Registration real, ver `DEPLOY.md`) — la parte que sí se verificó end-to-end es toda la lógica propia (restricción de rol, comportamiento inerte sin configurar).

## Fase 34 — Auto-provisión SSO sin rol, sala de espera, correo de notificación, y excepción de Administrador

✅ Completa (2026-07-18). Dos peticiones del usuario sobre la Fase 33, el mismo día: (1) en vez de rechazar el login de una cuenta de Microsoft no dada de alta, auto-provisionarla sin rol y ponerla en una sala de espera hasta que un Administrador le asigne uno — con notificación por correo en ambos sentidos; (2) que Administrador pueda seguir entrando siempre por login local con su rol real, para no perder el acceso al portal durante las pruebas de SSO.

- **Auto-provisión sin rol** (`IdentityEndpointsExtensions`, `/cuenta/microsoft-callback`): si el email de la cuenta de Microsoft no coincide con ningún `ApplicationUser`, se crea uno nuevo sin contraseña (solo login externo), sin ningún rol asignado. Restringido igual que el resto de SSO al tenant de la empresa (`AzureAdOptions.TenantId`) — nunca "cualquier cuenta Microsoft".
- **`ApplicationUser.FechaCreacion`** (nuevo, migración `AgregarFechaCreacionUsuario`): Identity no trae esto de serie: se usa para ordenar la lista de pendientes.
- **Sala de espera** (`PendienteDeRol.razor`, `/cuenta/pendiente-de-rol`): mensaje fijo ("Asignación de rol pendiente… Será asignada por el administrador en los próximos momentos"), diseño visual pendiente de definir por el usuario — de momento solo el mensaje pedido, con un botón de cerrar sesión. Se llega ahí desde el propio callback de Microsoft, y `MainLayout` la hace cumplir en cada navegación (mismo mecanismo ya usado para `DebeCambiarContrasena`) para que no se pueda saltar escribiendo otra URL.
- **Pestaña "Pendientes de asignar"** (`Roles.razor`, dentro de la pantalla de Roles): lista los `ApplicationUser` sin ningún rol (email, nombre, fecha de solicitud), con un selector de rol y un botón "Asignar" por fila. Para Gestor CAE o Cliente con sus vínculos completos (coordinador, cliente asociado), el flujo es asignar aquí un rol simple y terminar de configurarlo después en Usuarios — no se duplicó el formulario completo de alta.
- **`IEmailService`/`GraphEmailService`** (`Application/Common`, `Infrastructure/Email`, nuevo — construye parte de lo que pedía el Issue #2): Microsoft Graph con permisos de **aplicación** (`Mail.Send`, client credentials — no delegados, no depende de que haya un usuario con sesión iniciada, a diferencia del login SSO). Sin `Graph:TenantId/ClientId/ClientSecret/BuzonRemitente` configurados, queda inerte — las acciones que disparan el correo (crear pendiente, asignar rol) siguen funcionando igual, solo sin notificación, verificado explícitamente. Sin caché de token (se pide uno nuevo por envío): el volumen esperado no lo justifica.
- **Notificaciones**: al auto-provisionar un pendiente, se avisa por correo a todos los `ApplicationUser` en rol Administrador (solo la primera vez, no en cada login mientras siga pendiente). Al asignar un rol desde la pestaña de pendientes, se avisa por correo al usuario. **Plantilla de contenido pendiente de definir** con el usuario — de momento HTML mínimo sin diseño.
- **Excepción de Administrador** (`RestriccionLoginLocalClaimsTransformation`): un usuario con rol Administrador conserva su rol real incluso iniciando sesión por contraseña local, aunque Entra ID esté configurado — el resto de roles editores sigue capado a Consulta por login local.
- **Verificado en navegador real, con un bug real encontrado y corregido en el camino**: el primer intento de prueba insertó un usuario "pendiente" simulado directamente por SQL con un GUID en minúsculas — la asignación de rol fallaba con `FOREIGN KEY constraint failed` porque SQLite compara claves foráneas por texto exacto (case-sensitive) y los `Id` que crea la propia app vía EF/Identity se guardan en mayúsculas; no era un bug de la aplicación, sino del fixture de prueba (confirmado repitiendo la prueba con un usuario realmente creado por EF, sin el error). Con eso resuelto: cuenta pendiente creada aparece en la pestaña con contador correcto, asignación de rol funciona y quita al usuario de la lista, el fallo de envío de correo (Graph sin configurar) se registra en el log sin romper nada, la sala de espera redirige tanto al iniciar sesión como al intentar navegar directo a otra URL, y Administrador con login local sigue entrando a `/usuarios` mientras otro rol de prueba con login local queda bloqueado (excepción confirmada). `dotnet build`/`format`/153 tests en verde.

## Fase 35 — Lectura IA por tipo de documento (jerarquía Admin/Gestor) + notificaciones persistentes de cartera

✅ Completa (2026-07-18). Primeras dos partes ("C" y "D") de una petición más grande del usuario sobre lectura automática de documentos por IA (OCR de trabajadores en documentos de Empresa tipo ITA/RNT) — la extracción en sí y la UI de alta/baja de trabajadores quedan explícitamente pendientes ("A" y "B", no pedidas todavía). Lo que sí se pidió y se construyó ahora es el interruptor de dos niveles para activar/desactivar la lectura por IA por tipo de documento, y el mecanismo de aviso para que un Gestor CAE nunca se encuentre cambios de cartera o de configuración heredada sin explicación.

- **`TipoDocumento.LecturaIaActiva`** (nuevo, `true` por defecto): interruptor de Nivel 1, solo Administrador (`ActualizarLecturaIaGlobalCommand`, comprobación de rol explícita — no hay concepto de "semi-admin" aquí). Columna añadida en `TiposDocumento.razor` con checkbox inline.
- **`ConfiguracionIaDocumentoCliente`** (nuevo, `Domain/Documentos/`): override de Nivel 2 por Cliente + TipoDocumento, gestionable por Gestor CAE (o roles superiores) solo sobre los Clientes visibles en su alcance de datos (`IAlcanceDatosService`, mismo mecanismo de la Fase 31). Modelado como tabla dispersa a propósito: la ausencia de fila significa "hereda el valor global" — así una reasignación de cartera no necesita ningún código especial para que la configuración previa sobreviva, ya que la fila está indexada por `ClienteId`, no por el Gestor que la creó. `EfectivaActiva = GlobalActiva && (OverrideActiva ?? true)`.
- **`ConfiguracionIaCliente.razor`** (`/clientes/{id}/lectura-ia`, nueva): tabla con columnas Nivel 1/Nivel 2 por tipo de documento, checkbox de Nivel 2 deshabilitado si Nivel 1 ya está desactivado (no tiene sentido activar un override sobre algo apagado globalmente).
- **`NotificacionUsuario`** (nuevo, `Domain/Notificaciones/`) + **`NotificacionesPopup.razor`** (montado en `MainLayout`, isla interactiva `@rendermode InteractiveServer` propia): al iniciar sesión, un Gestor CAE ve en un modal cada notificación pendiente una a una, con botón "Omitir" siempre y "Gestionar" cuando la notificación trae una URL de acción — construido genérico (no acoplado a los dos casos de uso de abajo) para poder reusarse en el futuro.
- **Reasignación de cartera** (`ReasignarEjecutivoClienteCommand`, nuevo — y un campo mínimo "Gestor CAE asignado" añadido al drawer de edición de `Clientes.razor`, visible solo para Administrador/DireccionCae/CoordinadorCae): dispara dos avisos independientes cuando corresponde — al Gestor anterior ("se te ha quitado el cliente X") y al nuevo (siempre "se te ha asignado el cliente X"; además, si el Cliente tiene algún tipo de documento con la IA desactivada a nivel de Cliente, un segundo aviso "Cliente con documento X sin lectura automática" con botón "Gestionar" que lleva directo a `/clientes/{id}/lectura-ia`). No se construyó la pantalla de Supervisión completa (fuera de alcance de "C" y "D") — este campo es la mínima vía para poder disparar y probar el flujo de notificaciones de extremo a extremo hoy.
- **Verificado en navegador real con Playwright** (no solo tests unitarios): Administrador desactiva la lectura IA de un tipo de documento globalmente y persiste tras recargar; se reasigna un Cliente de un Gestor a otro; el Gestor anterior ve el aviso de pérdida; el nuevo Gestor ve primero el aviso de alta de cartera y, al descartarlo, el aviso de configuración IA heredada; el botón "Gestionar" navega a la pantalla de configuración y muestra "Desactivada para todos" en Nivel 1; tras gestionar la notificación, no vuelve a aparecer en una recarga posterior.
  - Dos bugs reales de navegación encontrados y corregidos en el camino, ambos en `NotificacionesPopup.razor`: `NavigationManager.NavigateTo()` sin `forceLoad: true` no surtía efecto — el popup es una isla interactiva aislada del resto de la página (que puede estar renderizada de forma estática), así que la navegación SPA no tenía un router en el mismo circuito para atenderla; con `forceLoad: true` (mismo patrón ya usado en `Login.razor`/`MainLayout.razor.cs` para transiciones que cruzan el límite de autenticación) la navegación sí se propaga como una carga de página real.
  - Dos bugs de guion de prueba (no de la aplicación), documentados por transparencia: el localizador `tr:has-text("ITA")` de Playwright hacía *substring match* insensible a mayúsculas, así que coincidía con las 20 filas de la primera página de la tabla porque el botón "Editar" de cada fila contiene la subcadena "ita"; y (corrección posterior, ver Fase 36) el guion había concluido erróneamente que "ITA" no existía en absoluto en los datos sembrados — sí existe (`TipoDocumentoSeedData`, Empresa, orden 18, junto con "RNT/TC2" en orden 22), solo que ambos caen en la segunda página de `/tipos-documento` y por eso no aparecían en el volcado de depuración, que solo leía la página visible. Se cambió la prueba a "ITC" (Vehículo, sí visible en la página 1) para evitar tocar paginación en el propio guion, sin que eso invalide el resultado — ver Fase 36 para dónde sí importó tener el dato correcto.
- `dotnet build`/`format`/168 tests en verde (109 Domain + 28 Application + 18 IntegrationTests + 7 Web.Tests + 6 E2ETests).
- **Fuera de alcance de esta fase, explícitamente no pedido todavía**: la extracción real de trabajadores por IA/OCR de documentos ("A"), y la UI de confirmación de alta/baja de trabajadores detectados ("B").

## Fase 36 — Detección de altas/bajas de personal por IA en documentos de Empresa ("A" y "B")

✅ Completa (2026-07-19). Partes "A" (extracción por IA) y "B" (UI de confirmación) de la petición de la Fase 35, ahora sí construidas: lectura automática por IA de documentos de Empresa que traen un listado de personal (ITA, RNT/TC2), comparación contra los Trabajadores activos de esa misma Empresa, y una pantalla de revisión donde un Gestor CAE decide, uno por uno, si da de alta a cada trabajador nuevo detectado o da de baja a cada trabajador que ya no aparece — nada ocurre automáticamente.

- **`TipoDocumento.DeteccionTrabajadoresActiva`** (nuevo, `false` por defecto — a propósito, ver comentario en el propio campo): solo Administrador puede activarlo, solo para tipos de documento de Ámbito Empresa (`ActualizarDeteccionTrabajadoresGlobalCommand`). La mayoría de documentos de Empresa (certificados, seguros, mutua...) no traen ningún listado de personal, así que actívarlo por defecto habría generado llamadas a IA y "detecciones" sin sentido — el seed solo lo activa para "ITA" y "RNT/TC2", los dos tipos reales que sí traen un listado (ver más abajo, corrección sobre la Fase 35: **ambos ya existían** en el catálogo, `TipoDocumentoSeedData`, ámbito Empresa, órdenes 18 y 22 — la duda de la fase anterior sobre si "ITA" existía era un error del guion de prueba, no de los datos).
- **`IExtraccionTrabajadoresIaService`/`AnthropicExtraccionTrabajadoresIaService`** (`Application/Common`, `Infrastructure/AsistenteIa`, nuevo): reutiliza `AnthropicOptions` (misma ApiKey que el chat "Pregúntale a Hydra") — mismo patrón "inerte por defecto". El PDF se envía como bloque de contenido `document` (Claude soporta PDFs nativamente, sin convertir a imágenes por página) con un prompt que exige JSON estricto `[{"nombre","apellidos","dni"}]`; el parseo busca el primer `[`/último `]` como red de seguridad por si el modelo envuelve la respuesta en markdown pese a la instrucción.
- **`DeteccionTrabajador`** (nuevo, `Domain/Trabajadores/`): fila pendiente por cada discrepancia — `Nuevo` (aparece en el documento, sin alta en el sistema) o `Ausente` (de alta en el sistema, ya no aparece). `DeteccionTrabajadoresService` (Application, orquestador puro — no toca nada de Infrastructure, así que se registra en la propia Application, no en Infrastructure) hace el diff por DNI normalizado y evita duplicar si ya hay una detección pendiente sin resolver para el mismo Documento.
- **Alcance deliberadamente restringido, tal como pidió el usuario en la Fase 35**: solo se comparan Trabajadores con `EmpresaId` igual al de la Empresa del documento — nunca de Subcontrata (tiene su propia documentación separada, con su propio listado de personal). Los trabajadores extranjeros no requieren ningún filtro explícito: simplemente no aparecen en documentos de Seguridad Social española, así que nunca generan una detección — es una consecuencia natural del propio documento, no una regla añadida al código.
- **Nivel 2 (Gestor CAE) también respetado aquí**: si la Empresa presta servicio a Clientes y *todos* esos Clientes tienen la lectura IA de ese TipoDocumento desactivada a nivel de Cliente (`ConfiguracionIaDocumentoCliente`, Fase 35), la detección no se ejecuta. Si basta con que uno solo la tenga activa (o no exista ninguna Empresa-Cliente vinculada) para que se ejecute — una Empresa puede servir a varios Clientes con configuraciones distintas, y no tiene sentido negarle la detección a todos por la preferencia de uno.
- **Aviso al Gestor** (reutiliza `NotificacionUsuario`, Fase 35): al detectar cambios, se notifica a cada Gestor CAE distinto entre los Clientes vinculados a la Empresa, con `UrlAccion` a la nueva pantalla de revisión — mismo mecanismo de pop-up ya construido, sin código nuevo de notificación.
- **`DeteccionTrabajadores.razor`** (`/empresas/{id}/deteccion-trabajadores`, nueva): dos tablas, "Trabajadores nuevos detectados" (botones Descartar / Dar de alta) y "Trabajadores ausentes" (botones Mantener activo / Dar de baja). `ResolverDeteccionNuevoCommand`/`ResolverDeteccionAusenteCommand` (nuevos) hacen el alta (`Trabajador.DeEmpresa`, con los mismos datos extraídos por IA) o la baja (`MarcarComoEliminado`, soft delete ya existente en `EntidadBase`) respectivamente — nunca de forma automática, siempre tras la decisión explícita del Gestor en pantalla.
- **Hook de disparo**: `CrearDocumentoCommandHandler` llama a `DeteccionTrabajadoresService` justo después de guardar un Documento de Empresa cuyo TipoDocumento tenga la detección activa — "mejor esfuerzo": cualquier fallo (sin ApiKey, error de red, PDF no legible) se registra en el log y nunca impide que la subida del propio Documento se complete. Verificado explícitamente en navegador real: subir un ITA sin `Anthropic:ApiKey` configurada no rompe nada, simplemente no genera detecciones.
- **Verificado en navegador real con Playwright, incluida la resolución completa**: toggle de "Detecta trabajadores" visible y activo por defecto para ITA; subida de un documento ITA real (PDF generado con LibreOffice, no un archivo inventado) sin API key configurada, sin errores; pantalla de revisión cargada con detecciones sembradas directamente en SQLite (mismo criterio que la Fase 34, ya que no hay forma de probar el modelo de visión real en este entorno sin una ApiKey real) — "Dar de alta" crea de verdad un `Trabajador` nuevo con los datos detectados, "Dar de baja" desactiva de verdad al trabajador existente, y ambas filas desaparecen de la lista de pendientes tras resolverse.
  - Un detalle real y positivo encontrado durante la verificación, no un bug: el primer intento de prueba sembró un DNI con dígito de control inválido (`55667788K`, el correcto para ese número es `Z`) — `ResolverDeteccionNuevoCommand` lo rechazó correctamente con `Deteccion.DatosInvalidos` en vez de crear un Trabajador con datos corruptos, confirmando que la validación de `Trabajador.EstablecerDni` protege también este camino de alta automática. El mensaje de error contiene la palabra "alta" ("...puedes darlo de alta manualmente..."), lo que de paso hizo que un primer aserto de prueba demasiado laxo (`hasText: /alta/i` sobre todo el Toast) diera un falso positivo — corregido apuntando al texto exacto del `<span>` interno del Toast, no al Toast completo con el botón de cerrar.
- `dotnet build`/`format`/182 tests en verde (109 Domain + 39 Application + 21 IntegrationTests + 7 Web.Tests + 6 E2ETests).
- **Fuera de alcance, explícitamente no pedido**: ningún cambio en el catálogo general de tipos de documento más allá de los dos nuevos campos de esta fase — ver Fase 37 para la ampliación del catálogo a partir del listado de documentación CAE que pasó el usuario.

## Fase 37 — Ampliación del catálogo de tipos de documento (CAE/PRL España)

✅ Completa (2026-07-19). El usuario pasó un listado extenso de documentación CAE/PRL usada en España (clasificada por Empresa local/extranjera, Subcontrata, Trabajador local/UE/no-UE, Maquinaria, Obra, y por actividad), pidiendo ampliar con él el catálogo real de `TipoDocumento`.

- **Añadidos 21 tipos nuevos de Ámbito Trabajador** (prefijo de Id `50000000-...`, Orden 16-36): Contrato de Trabajo, Alta en Seguridad Social, Formación Riesgos Específicos, Formación EPIs, Permiso de conducir, Riesgo Eléctrico (36 meses), Manipulación Manual de Cargas, Manipulación de Productos Químicos, ADR (60 meses), Soldadura, seis tipos de Operador de maquinaria (Puente Grúa, Grúa Torre, Grúa Móvil, Dumper, Retroexcavadora, Minicargadora, Manipulador Telescópico), Permiso de residencia, Permiso de trabajo, Certificado de Registro de Ciudadano de la UE, Certificado A1 de Seguridad Social.
- **Añadidos 17 tipos nuevos de Ámbito Empresa** (prefijo de Id `40000000-...`, Orden 29-45): Plan de Prevención, Designación de Recursos Preventivos, Procedimiento de Coordinación de Actividades Empresariales (estos tres marcados `EsObligatorio`, igual criterio que EVR/PAP/SPA ya existentes), Política Preventiva, Organigrama Preventivo, Modalidad Preventiva, Escritura de Constitución, Poder del Representante Legal, ISO 45001/9001/14001, Declaración Responsable CAE, Relación de Maquinaria, VAT europeo, Documento acreditativo de empresa extranjera, Traducción jurada, Comunicación de desplazamiento.
- **Deliberadamente NO añadidos, fuera de alcance de esta fase** (comunicado al usuario antes de empezar): documentación a nivel de Subcontrata, y de Maquinaria/Obra como entidades propias — el modelo de datos actual no tiene esos "ámbitos" (`Documento` solo se asocia a Trabajador/Cliente/Empresa/Vehículo; `AmbitoAplicacion` no tiene valores `Subcontrata`/`Maquinaria`/`Obra`). Añadirlos habría exigido extender el propio modelo de dominio (nuevos valores de enum, nuevos factory methods en `Documento`, migración de esquema), no solo sembrar filas — se deja como trabajo futuro explícito si el usuario lo pide. Tampoco se añadió DNI/NIE/Pasaporte como `TipoDocumento`: son datos de identidad ya capturados por `Trabajador.Dni`, no documentos que se suban — mismo criterio que ya seguía el catálogo original (nunca tuvo un "DNI" como tipo de documento).
- Sin cambios de vigencia en los tipos ya existentes — se respetó tal cual el catálogo de la Fase 0 para no alterar comportamiento en producción; solo se documentó, cuando el listado del usuario daba una vigencia orientativa razonable para un tipo *nuevo*, en su propia nota.
- `dotnet build`/`format`/182 tests en verde — el único ajuste necesario fue el número total de tipos de documento sembrados en `MigracionesTests` (de 32 a 70).

## Iniciativa de hardening — CI/CD, tests E2E, multi-tenant, seguridad, observabilidad, RGPD

Planteada por el usuario el 2026-07-17: antes de vender el producto a clientes corporativos reales hace falta resolver ocho frentes que hasta ahora no se habían tratado como bloque. Priorización explícita del usuario, **no en fila** — tres franjas según si son trabajo de ingeniería secuencial o decisiones que se pueden (y deben) resolver hoy en paralelo sin esperar a nada:

- **Franja 0 — bloqueante, primero de todo**: CI/CD y tests E2E automatizados. Todo lo demás se construye más rápido y con más seguridad si esto existe antes.
- **Franja 1 — en paralelo, no dependen de la Franja 0**: decisión multi-tenant, gestión de secretos/claves, observabilidad.
- **Franja 2 — se apoyan en las anteriores**: triage de backlog + infraestructura de email, RGPD/LOPD, proceso de cadencia de hardening (el mecanismo que evita que se repita el backlog de 13 puntos que generaron las Fases 7-23 sin este proceso).

### 1. CI/CD — ✅ completo (2026-07-18)

- ✅ `.github/workflows/ci.yml`, seis jobs en paralelo: `build-and-test` (`dotnet restore`, `dotnet build -warnaserror`, `dotnet format --verify-no-changes`, tests de Domain/Application/IntegrationTests), `migraciones-pendientes` (`dotnet ef migrations has-pending-model-changes`), `docker-build` (reconstruye la imagen real de despliegue), `bunit-tests`, `e2e-tests` (instala Chromium vía el `playwright.ps1` que genera el propio paquete `Microsoft.Playwright` y corre la suite E2E) y `gitleaks` (escaneo de secretos en cada push/PR).
  - Se instaló `dotnet-ef` como herramienta local (`.config/dotnet-tools.json`) para que el job de migraciones sea reproducible en un runner limpio.
  - Se corrigió `dotnet format` sobre todo el repo antes de activar el gate — arranca en verde.
  - No verificado en este entorno que los jobs `docker-build`/`e2e-tests`/`gitleaks` pasen tal cual en GitHub Actions (sin daemon Docker ni ejecución de Actions disponibles aquí) — sí verificados localmente en todo lo que no depende de esos dos entornos (build, format, los 4 proyectos de test corriendo de verdad, incluida la suite E2E completa contra la app real).
- ✅ **Las 3 piezas de panel, cerradas con el usuario paso a paso el 2026-07-18**: branch protection rule en `main` (classic, no ruleset — filtrada por fuente "GitHub Actions", exige los 6 checks del workflow + rama actualizada antes de mergear); entorno de staging en Railway (servicio duplicado del de producción, rama `staging` creada en GitHub apuntando a `main`, volumen y `AdministradorInicial` propios y verificados como no compartidos con producción — SQLite no tolera dos procesos escribiendo el mismo archivo); notificación de build roto vía el email de Actions que GitHub ya manda por defecto (usuario no usa Slack, así que no hizo falta el paso de webhook).
  - Corregido de paso un fallo real en GitHub Actions que no se veía en este entorno de desarrollo (verificado por separado del propio pipeline en verde local): `AngleSharp` 1.4.0 (transitivo de bunit) tiene una vulnerabilidad conocida que el chequeo de auditoría de NuGet solo detecta con acceso real a su fuente de datos — fijado a 1.5.2. Y `WebAppFixture.cs` tenía hardcodeada la ruta del Chromium de este sandbox (`/opt/pw-browsers/chromium`), que no existe en el runner de GitHub — ahora usa esa ruta solo si existe en disco, si no deja que Playwright resuelva el navegador que su propio `playwright install` descarga en CI.

### 2. Tests E2E automatizados — ✅ base construida y verificada (2026-07-18)

- ✅ `tests/CaeManager.E2ETests` (Playwright .NET): `WebAppFixture` arranca `CaeManager.Web.dll` como proceso real (no un servidor in-memory) contra una SQLite temporal sembrada con `DatosPrueba__Activo=true`, sondeando `/salud` hasta que las migraciones+siembra terminan — una sola instancia compartida por toda la colección de tests. `FlujoCriticoTests` cubre el camino crítico completo (login → Cliente → Empresa → Trabajador → Documento → semáforo "Urgente"), `AlcanceRolesTests` adapta a C# los 6 chequeos de rol que antes solo se verificaban a mano con `verificar_roles.js` (contexto de navegador nuevo por rol, sin depender de logout), `RegresionesTests` cubre el bug #7 del backlog (seleccionar texto dentro del drawer no lo cierra, clic genuino fuera sí). 6/6 en verde, corrido dos veces seguidas sin flakiness.
- ✅ `tests/CaeManager.Web.Tests` (bUnit): `CampoTextoTests` cubre el debounce de 300ms y el fix de `@onblur` (Fase de backlog #11 — flush inmediato al perder foco). `DrawerTests` cubre a nivel de componente el mismo bug de selección de texto que `RegresionesTests` cubre a nivel de navegador real. 5/5 en verde.
- **Diagnóstico real encontrado al verificar** (no un simple "flakiness" — errores concretos de la primera versión, corregidos): (1) `page.GotoAsync` hace una navegación real de navegador, no un enrutado cliente de Blazor — tira abajo el circuit de SignalR en cada llamada; interactuar justo después sin esperar a `networkidle` hacía clic en botones cuyo `@onclick` aún no estaba cableado del lado servidor (mismo patrón ya usado con éxito en `verificar_roles.js`, ahora centralizado en `Ayudas.NavegarYEsperarAsync`). (2) El test de creación de Empresa asumía que el drawer se cierra al guardar — en realidad `Empresas.razor.cs` pasa a modo edición a propósito tras crear (fix ya implementado del punto #3 del backlog, para dejar visibles las credenciales de acceso) — corregido para esperar el título "Editar empresa" y cerrar explícitamente. (3) Un `GetByLabel("Empresa")` ambiguo (coincidía con el radio "tipo de empleador" y con el `<select>`) — resuelto por rol (`Combobox` vs. `Radio`). (4) Generar el PDF de prueba con texto dibujado requería un `IFontResolver` que solo existe registrado en el proceso de la app (`EmbeddedFontResolver`, `Program.cs`), no en el proceso de test — simplificado a una página en blanco, igual de válida para probar el flujo de subida real.
- ⬜ **Regla de cadencia todavía no aplicada**: "ninguna fase nueva se cierra sin al menos un test E2E del flujo que añade" queda como intención documentada aquí, no como gate mecánico — eso es responsabilidad del punto 8 (proceso de cadencia), no de este punto.

### 3. Decisión multi-tenant — ✅ reactivada 2026-07-23 (`ADR-003-saas-multitenant.md`), implementación in-place en curso de planificación

**Actualización (2026-07-23)**: la vía SaaS multi-tenant vuelve a ser **el objetivo principal del producto** — `ADR-003` supersede la pausa de `ADR-002` (incluida su cláusula de fork: la implementación es in-place en este repositorio, con la organización actual como tenant #1). Documento normativo: `docs/MULTITENANCY.md` (aislamiento, catálogos global/por-tenant, Tenant Resolution Strategy); análisis y estrategia de migración por etapas: `INFORME-MULTITENANT.md`. Secuencia acordada: consolidación documental (✅ 2026-07-23) → aprobación → plan de migración detallado → implementación → validación. **El código todavía no tiene `TenantId`** — nada de la implementación ha empezado.

**Actualización (2026-07-18)** *(histórica, superseded)*: la vía SaaS quedó en pausa por `ADR-002` (uso interno, fork si se retomaba). El párrafo siguiente se conserva como registro de la decisión original.

Decisión original (2026-07-17), ver `ADR-001-multitenant.md`: el producto es **SaaS multi-cliente**, no uso interno de una sola organización — esto reemplaza el punto "Multi-tenant real" que estaba listado más abajo como fuera de alcance de v1. Modelo elegido: `TenantId` por fila + Global Query Filter de EF Core (extensión directa del patrón ya usado para `EstaEliminado`), no base de datos separada por tenant. Auditadas las 39 queries de `Application/*/Queries`: ninguna usa SQL crudo ni `IgnoreQueryFilters()`, así que el filtro global protegería las 39 a la vez sin tocar cada handler. Dos deudas concretas quedan documentadas en el ADR para cuando se implemente: sellar `TenantId` en escritura vía interceptor (no confiar en cada Command), y convertir 7 índices únicos hoy globales (`Cliente.Cif`, `Empresa.Cif`/`RazonSocial`, `Subcontrata.RazonSocial`, `TipoDocumento.Nombre`, `Trabajador.Dni`, `Vehiculo.NumeroPlaca`) en únicos compuestos `(TenantId, campo)` — un mismo Trabajador/Empresa real puede legítimamente ser cliente de dos organizaciones PRL distintas. Implementación (añadir la columna, el filtro, el interceptor, los índices compuestos) **todavía no empezada** — esto es la decisión, no la construcción. ([Issue #8](https://github.com/christopherjp1-jpg/Project-Hydra/issues/8))

### 4. Gestión de secretos y claves de cifrado — 🟡 en marcha (2026-07-18)

- ✅ 2FA (TOTP) para cualquier usuario autenticado, con acceso desde el menú de Administración: `/cuenta/configurar-2fa` (clave compartida + URI `otpauth://` para escanear o introducir a mano, patrón estándar de ASP.NET Core Identity — `GetAuthenticatorKeyAsync`/`VerifyTwoFactorTokenAsync`/`SetTwoFactorEnabledAsync`) y `/cuenta/verificar-2fa` (challenge de login cuando el usuario ya tiene 2FA activo). No hay enrolamiento forzado retroactivo — queda disponible para quien quiera activarlo, empezando por Administrador.
- ✅ `ApplicationUser.DebeCambiarContrasena`: se marca `true` al crear un usuario desde `/usuarios` (contraseña temporal elegida por el Administrador), `false` para el Administrador inicial (`IdentitySeeder`) y los usuarios de `DatosPruebaSeeder` (contraseñas conocidas y deliberadas, no temporales de un tercero). Se hace cumplir en dos capas: `Login.razor` redirige justo tras autenticar (evita el salto visible), y `MainLayout.OnParametersSetAsync` vuelve a comprobarlo en cada navegación (no se puede saltar escribiendo otra URL a mano) — `/cuenta/cambiar-contrasena` usa `AuthLayout` en vez de `MainLayout`, así que nunca se autobloquea. Verificado en navegador: un usuario recién creado con contraseña temporal es forzado a `/cuenta/cambiar-contrasena`, un intento de navegar directo a `/clientes` rebota de vuelta, y tras cambiarla entra con normalidad.
- ✅ `RUNBOOK-CLAVES.md`: qué se pierde exactamente si se pierde el volumen de Railway — el hallazgo central es que perder solo `dataprotection-keys/` (aunque `CaeManager.db` sobreviva) deja las credenciales de acceso a plataformas externas (`CredencialAccesoEmpresa`/`CredencialAccesoSubcontrata`) cifradas de forma permanentemente irrecuperable, no solo "las sesiones se cierran" — por eso cualquier backup tiene que copiar la base de datos y las claves juntas, como una unidad atómica, nunca por separado.
- ✅ Escaneo de secretos (`gitleaks`) añadido al pipeline de CI del punto 1.
- ✅ Auditado `appsettings.json`: no versiona ningún secreto real (solo el connection string de SQLite local de desarrollo).
- ⬜ **Sin empezar todavía**: migrar las claves de Data Protection a un almacén gestionado (Azure Key Vault/AWS KMS) en vez de archivos en el volumen; confirmar en el panel de Railway que `AdministradorInicial:Contrasena` está puesta a un valor real (no se puede verificar desde este entorno); política de expiración/rotación de contraseñas más allá del primer cambio forzado.

### 5. Observabilidad — 🟡 en marcha (2026-07-18)

- ✅ Logging estructurado con Serilog: sustituye al proveedor por defecto de `Microsoft.Extensions.Logging` (la sección `Logging` del `appsettings.json` se reemplaza por `Serilog`, mismo mecanismo de variables de entorno que el resto de la app). Sink de consola + archivo con rotación diaria (`App_Data/logs/log-.txt` por defecto, configurable con `Logging:RutaArchivo`), `UseSerilogRequestLogging()` registrado para capturar cada petición HTTP con su código de estado final. Verificado en local: arranca limpio, escribe tanto a consola como a archivo, incluidas las peticiones reales. Preparado (documentado, sin paquete instalado todavía porque no hay cuenta provisionada) para añadir un sink en la nube (Seq/Axiom) el día que haga falta, con el mismo patrón "funciona sin configurar, se endurece con una variable de entorno" que ya usan `DataProtection`/`AlmacenamientoArchivos`.
- ✅ Error tracking con Sentry (`Sentry.AspNetCore`), inerte por diseño mientras `Sentry:Dsn` no esté configurado — ninguna cuenta de Sentry provisionada todavía, así que hoy no envía nada en ningún entorno. Convive con el `UseExceptionHandler("/Error", ...)` ya existente (`ARCHITECTURE.md`): captura la excepción para reportarla y la deja seguir su curso normal hacia la página de error genérica.
- ✅ **Backups automatizados a S3, implementado 2026-07-18** (`BackupHostedService`, ver `RUNBOOK-CLAVES.md` § Prevención para el detalle completo): sube `CaeManager.db` + `dataprotection-keys/` juntos cada `Backups:IntervaloHoras` (24h por defecto) usando el backup online de SQLite (`SqliteConnection.BackupDatabase`, no bloquea escrituras). Apagado por defecto, mismo patrón que `DatosPrueba:Activo`. Verificado en local con credenciales de AWS falsas: arranca, genera correctamente el backup de la BD y el zip de claves, y falla de forma controlada (log de error, sin tumbar la app) solo en el paso de subida — no se pudo verificar una subida real a S3 porque el usuario todavía no tiene el bucket final creado (bloqueado por un error transitorio de AWS al recrear un nombre de bucket recién borrado). **Restauración probada**: documentada paso a paso en `RUNBOOK-CLAVES.md`, pero no verificada de extremo a extremo contra un bucket real por el mismo motivo — pendiente de una prueba real en cuanto el bucket exista.
- ⬜ **Sin empezar todavía**: métricas de negocio (documentos vencidos sin gestionar, tiempo de respuesta del Dashboard, tasa de fallo de importaciones); alertas activas si `/salud` falla o el volumen de Railway se acerca al límite; panel de "salud del sistema" para Administrador dentro de la propia app (última siembra, última importación, errores recientes); decidir si/cuándo provisionar una cuenta de Sentry real y a qué sink de logs en la nube apuntar. ([Issue #9](https://github.com/christopherjp1-jpg/Project-Hydra/issues/9))

### 6. Backlog priorizado + infraestructura de email — 🟡 decidido, sin construir (2026-07-18)

El "Backlog pendiente" de más abajo en este documento ya distingue bugs (mayoría resueltos) de features de diseño pendientes de decisión — ver [Issue #2](https://github.com/christopherjp1-jpg/Project-Hydra/issues/2) (email de bienvenida + alertas de vencimiento). El diseño del `Documento` a nivel Cliente/Empresa ya se implementó (Fase 23/punto 9 del backlog); el piloto de drawers persistentes sigue pendiente ([Issue #1](https://github.com/christopherjp1-jpg/Project-Hydra/issues/1)).

**Decisión tomada con el usuario**: `IEmailService` se construye sobre **Microsoft Graph API**, no SMTP/SendGrid — no solo por las licencias de M365 ya pagadas, sino porque Graph es la única opción que sirve tanto para enviar como para (más adelante) suscribirse a correo entrante con las mismas credenciales, necesario para la Iniciativa de IA (§7b, sistema de tickets). Empezar con SMTP habría significado reescribir esta capa cuando se llegue a esa etapa.

**Pruebas**: el usuario no tiene una cuenta de administrador de M365 todavía, así que las primeras pruebas se hacen con una cuenta personal de Hotmail/Outlook.com — misma API de Graph, pero con *delegated permissions* (consentimiento interactivo del propio usuario la primera vez, con token de refresco guardado) en vez de *application permissions* (autenticación desatendida, sin login). Cuando se disponga del buzón compartido real de la organización, se cambia el mecanismo de autenticación al modo de aplicación — el diseño de `IEmailService` no cambia, solo la implementación de autenticación por debajo.

### 7. Cumplimiento normativo (RGPD/LOPD) — 🟡 retención decidida, resto pendiente (2026-07-18)

- ✅ **Documento de tratamiento de datos redactado** (`RGPD-TRATAMIENTO-DATOS.md`) — qué PII se almacena y dónde, base legal, quién accede, subencargados (Railway/AWS/Sentry/Graph/Anthropic), y el hallazgo del campo `Trabajador.Observaciones` como texto libre sin categorizar que podría acabar conteniendo datos sensibles sin que el sistema lo sepa. No sustituye revisión legal.
- ✅ **Retención de Documentos: 5 años, decidido por el usuario.** Falta implementar el mecanismo real (hoy el soft-delete vía `EstaEliminado` guarda todo indefinidamente, sin ningún job que aplique el plazo) — ver [Issue #10](https://github.com/christopherjp1-jpg/Project-Hydra/issues/10) para los puntos todavía abiertos (desde qué fecha se cuentan los 5 años, si aplica igual a los tres ámbitos de Documento, y la retención del propio `Trabajador` como entidad, que sigue sin decidir).
- ⬜ Pendiente: documento de tratamiento de datos (qué PII se almacena — DNI/NIE, datos médicos PRL —, base legal, quién accede, retención); mecanismo real de derecho al olvido (hoy el global query filter oculta, no borra — [Issue #11](https://github.com/christopherjp1-jpg/Project-Hydra/issues/11)); confirmar que Railway termina TLS end-to-end sin tramo en claro (HSTS/ForwardedHeaders ya están desde Fase 21); extender el registro de accesos (hoy solo cubre credenciales de plataforma, `ARCHITECTURE.md`) a quién vio qué dato sensible de qué Trabajador y cuándo ([Issue #12](https://github.com/christopherjp1-jpg/Project-Hydra/issues/12)); DPA si se factura como SaaS a terceros — bloqueante comercial, no técnico ([Issue #13](https://github.com/christopherjp1-jpg/Project-Hydra/issues/13)).

### 7b. Iniciativa de IA — Etapa 1 del chat construida y verificada (2026-07-18)

Tres integraciones de IA definidas en la misma conversación, todas sobre la API de Anthropic (Claude) — se documentan aquí para que la elección de Microsoft Graph (§6) y el diseño de `IEmailService` no se hagan sin tener en cuenta hacia dónde va esto después, mismo motivo por el que se prioriza no construir el dominio de "Ticket" todavía (evitar abstracción prematura, `CODING_STANDARDS.md`).

1. **[Issue #14] Extracción automática de reconocimientos médicos recibidos por correo**: buzón compartido → Graph webhook detecta correo entrante → PDF adjunto se manda directo a la API de Anthropic (lectura nativa de PDF, sin OCR separado) pidiendo extracción estructurada (trabajador, empresa, resultado, fecha de validez) → se busca coincidencia real en la base de datos. Regla no negociable: solo se archiva solo con alta confianza; cualquier duda va a una bandeja de revisión humana — un falso "Apto" mal leído no es un bug cualquiera en este dominio. Sin empezar todavía.
2. **[Issue #15] Sistema de tickets por correo con SLA** (tipo Zendesk) sobre uno o varios buzones conectados — por esto se eligió Graph API (permite enviar y suscribirse a correo entrante con las mismas credenciales) en vez de SMTP/SendGrid para el email de bienvenida del backlog. Sin empezar todavía.
3. **[Issue #16] Chat "Pregúntale a Hydra"** — ✅ **Etapa 1 construida y verificada**: botón flotante (`BotonAsistenteIa`, solo se renderiza con `Anthropic:ApiKey` configurada — inerte por defecto, mismo patrón que Sentry/Backups) + panel de chat (`AsistenteIa`) sobre `IAsistenteIaService`/`AnthropicAsistenteIaService` (Infrastructure, HTTP directo a la API de Anthropic con `claude-sonnet-5` por defecto, configurable). System prompt final aportado por el usuario (especialista "PRL Expert AI", formato estructurado ## Respuesta/## Base legal/## Recomendación, rechaza temas fuera de PRL/CAE, nunca inventa artículos/leyes, pide contexto cuando la respuesta depende de tipo de empresa/actividad/CCAA), con una restricción adicional propia de la plataforma: sin acceso a ningún dato real de Clientes/Trabajadores/Documentos. Respuestas renderizadas como markdown real (`Markdig`, con `DisableHtml()` para que HTML crudo en una respuesta —p. ej. por un intento de inyección de instrucciones— se trate como texto literal, no se ejecute). Verificado en navegador con una key inválida: el botón aparece solo con la key puesta, el panel abre y envía correctamente, y un fallo de la API (401) se muestra como error controlado sin tumbar la app. 140 tests en verde (4 nuevos: 2 del handler con servicio falso, 2 del render condicional del botón). Etapa 2 (futura, todavía sin construir): contexto de "lo que estoy viendo" — requerirá la misma revisión de qué datos es seguro mandar a una API externa que ya se le dio a RGPD en esta sesión.

Todas dependen de que el usuario provisione una cuenta/API key de Anthropic, y las dos primeras además de la app de Entra ID para Graph (ver §6).

### 8. Proceso de cadencia (hardening intercalado) — ✅ regla escrita 2026-07-18

**Regla de estabilización**: cada 5-6 fases de producto nuevas, la fase siguiente se dedica exclusivamente a deuda — no features. Alcance de una fase de estabilización: (a) cerrar cualquier test E2E/bUnit pendiente de las fases anteriores (ver el punto del checklist más abajo, que existe justo para que esta deuda no se acumule tanto como en las Fases 7-23), (b) revisar y triar el "Backlog pendiente" de este documento, (c) `dotnet list package --outdated` en las 4 capas y actualizar lo que no rompa nada, (d) revisar que `ROADMAP.md` sigue siendo la fuente de verdad real del estado del proyecto, no un historial desactualizado.

**Definition of done, ahora ejecutable** (ver `CODING_STANDARDS.md` → "Checklist de revisión", extendido con esto mismo): una fase no se da por cerrada solo con "verificado en navegador" — hace falta además (1) al menos un test automatizado (E2E o bUnit) del flujo que añade, si toca un flujo de usuario o componente no trivial, (2) los 6 checks de `ci.yml` en verde, (3) la entrada correspondiente en `ROADMAP.md` escrita antes de considerar la fase terminada, no después.

**Backlog → GitHub Issues**: hecho el 2026-07-18, ver enlaces en la sección "Backlog pendiente" más abajo — cada punto sin resolver tiene ahora su Issue en `christopherjp1-jpg/Project-Hydra` con label `bug`/`feature`/`decision-needed`, en vez de vivir solo como texto en este archivo.

**Umbral de calidad** (reescrito 2026-07-23 tras `ADR-003-saas-multitenant.md` — dos umbrales secuenciales, el segundo ya no es hipotético):

- **Umbral para la operación actual (tenant #1, ya exigible)**: los 6 checks de CI en verde de forma sostenida (no solo "en algún momento pasaron"); 0 bugs críticos abiertos en Issues; corrección del control de acceso por cartera en las consultas `*PorId*` y en la descarga de archivos (hallazgo IDOR — bloqueante ya, sin esperar a la venta); política de retención de datos y documento de tratamiento de datos resueltos (sección 7) — el RGPD/LOPDGDD aplica igual, incluida la categoría especial de salud.
- **Umbral de salida a producción SaaS (bloqueante para el segundo tenant real — ver `ADR-003` § condiciones)**: todo lo anterior más `TenantId` implementado y **auditado con tests de aislamiento por agregado** según `ADR-001`/`docs/MULTITENANCY.md`; índices únicos compuestos; archivos particionados por tenant; migración a PostgreSQL; DPA y términos de uso por tenant (con revisión legal); y sin self-signup ni billing antes de todo lo anterior. Un cliente corporativo real puede pedir evidencia de todo esto en due diligence antes de firmar.

## Backlog pendiente — reportado por el usuario el 2026-07-16, sin implementar

⏳ Sin empezar. Lista de bugs/mejoras reportados tras probar Fase 23 en Railway, para retomar en un chat nuevo. Cada punto incluye el síntoma tal como lo reportó el usuario y, donde ya hay una hipótesis técnica clara, una nota de diagnóstico — pero ninguno se ha investigado a fondo todavía salvo que se diga explícitamente.

1. **Datos de prueba no sembrados en Railway**: con `DatosPrueba__Activo=true` puesto en Railway, la base sigue sin las filas de prueba (200 Clientes, etc. de `DatosPruebaSeeder`). Verificado localmente que sí funciona (ver Fase 23) — revisar si la variable de entorno llegó bien al servicio, si el deploy corrió sobre una base que ya tenía Clientes (el seeder se auto-omite si `dbContext.Clientes.AnyAsync()`), o logs de arranque en Railway.
    - **Investigado (2026-07-16)**: revisado `DatosPruebaSeeder.SeedAsync` y `Program.cs` — el código en sí es correcto (`configuration.GetValue<bool>("DatosPrueba:Activo")`, que sí mapea correctamente desde la variable de entorno `DatosPrueba__Activo` por la convención estándar de .NET) y se ha verificado localmente en este mismo entorno que siembra sin problemas (ver los commits anteriores de esta sesión, sembrado repetidas veces sin fallos). **Gap real encontrado y corregido**: cuando el flag se lee como `false` (por el motivo que sea — variable no definida, mal escrita, no aplicada al servicio, o el valor no es literalmente "true"/"false"), el seeder no dejaba **ningún rastro en los logs** — indistinguible de otros fallos silenciosos. Añadido un log informativo con el valor exacto leído, para poder diagnosticar esto la próxima vez revisando los logs de arranque de Railway. No se puede acceder a Railway desde este entorno — pasos para el usuario: redeploy con el fix, y en los logs de arranque buscar una de estas tres líneas: (a) `"DatosPrueba:Activo no está activado (valor leído: 'X')"` → confirma que la variable no está llegando como `true` (revisar el nombre exacto `DatosPrueba__Activo` en el panel de Railway, sin comillas ni espacios); (b) `"DatosPrueba:Activo está en true, pero ya hay Clientes"` → la base ya tenía datos de un deploy anterior, hay que vaciarla o aceptar que no resembrará; (c) `"Sembrando base de datos genérica de prueba…"` seguido de `"Datos de prueba sembrados: ..."` → el seeder corrió correctamente, el problema estaría en otro sitio (¿se está mirando la base de datos correcta? ¿hay más de un servicio/volumen en Railway?).

2. **Empresa: falta el campo CIF al crear**. Hoy `Empresa` (`CaeManager.Domain/Empresas/Empresa.cs`) solo tiene `RazonSocial` — nunca tuvo CIF, a diferencia de `Cliente` que sí lo tiene y lo valida con `ValidadorIdentificacion`. Hay que decidir: ¿se añade `Cif` a `Empresa` (cambio de dominio + migración + toda la cadena CRUD), o el usuario se refiere a otra pantalla? Confirmar alcance antes de tocar el dominio.
    - **Hecho y verificado en navegador (2026-07-16)**: añadido `Cif` a `Empresa` como **opcional** (a diferencia de Cliente, donde es obligatorio) — decisión tomada sin preguntar porque ya hay 220 Empresas sembradas/importadas sin CIF y las plantillas de importación tampoco lo recogen todavía (mismo "fuera de alcance" que Cliente tuvo en su momento); hacerlo obligatorio habría roto esos flujos. Se valida el formato con `ValidadorIdentificacion` solo si se rellena. Cadena completa: migración EF (`AgregarCifAEmpresa`, columna `TEXT NULL` + índice único — SQLite permite múltiples `NULL` en un índice único sin conflicto), `IEmpresaRepository.ExisteConCifAsync` para bloquear duplicados, `CrearEmpresaCommand`/`EditarEmpresaCommand` con el nuevo parámetro y validación FluentValidation condicional (`.When(...)`), DTOs de listado y detalle, columna nueva en `Empresas.razor` (de paso se corrigió que la cabecera de Razón social no tenía `Title` y mostraba el nombre crudo de la propiedad) y campo en el formulario de alta/edición. 8 tests de dominio nuevos en `EmpresaTests.cs`. Instalado `dotnet-ef` como herramienta global para generar la migración. Verificado con Playwright: CIF inválido rechazado con mensaje, CIF válido guardado y persistido al reabrir, y la columna nueva convive bien con el fix de filas vacías del punto 4 (una Empresa sembrada sin CIF sigue mostrando su fila porque Razón social y Acciones no están vacías).

3. **Empresa: credenciales de acceso (Usuario/Contraseña/URL) solo aparecen al editar, no al crear**. Es el comportamiento actual **a propósito** (`Empresas.razor`: el bloque de credenciales está en `@if (_editandoId is not null)`, porque `GuardarCredencialAccesoEmpresaCommand` necesita un `EmpresaId` que no existe hasta guardar la Empresa por primera vez). El usuario quiere verlas desde el alta. Posible solución: guardar la Empresa al vuelo (sin cerrar el drawer) en cuanto se rellena la razón social, o mostrar los campos deshabilitados con una nota hasta el primer guardado. Mismo patrón replicado en Subcontratas — aplica ahí también.
    - **Hecho y verificado en navegador (2026-07-16)**: implementada la variante de menor riesgo — al pulsar el "Guardar" normal del alta (no autoguardado mientras se escribe, eso queda fuera de alcance), `GuardarAsync` ya no cierra el drawer tras crear: guarda `_editandoId` con el Id devuelto por `CrearEmpresaCommand`/`CrearSubcontrataCommand` (ambos ya devolvían `Result<Guid>`, sin cambios en Application) y el drawer pasa a "modo edición" en el sitio, revelando la sección de credenciales sin tener que volver a abrir el formulario desde la tabla. Al editar de verdad (segunda vez) sí se sigue cerrando como antes. Aplicado a `Empresas.razor.cs` y `Subcontratas.razor.cs`. Verificado con Playwright contra la app real (SQLite + datos de prueba): crear una Empresa deja el drawer abierto, el título cambia a "Editar empresa" y aparece la sección de credenciales.

4. **Tabla con filas vacías cuando hay pocos registros**: con 1 Empresa creada, el listado sigue renderizando `<tr><td class="col-justify-start"></td><td class="col-justify-end"></td></tr>` vacíos. Probablemente `QuickGrid` con `Virtualize` rellenando hasta el alto del contenedor, o un `ItemsPerPage`/paginación mal calculada con pocos elementos. Reproducir en `/empresas` con 1 fila y revisar el markup real generado.
    - **Confirmado y arreglado (2026-07-16)**: instalado el SDK de .NET en este entorno (vía `apt install dotnet-sdk-10.0`, no estaba disponible al principio de la sesión) y reproducido en navegador real con Playwright — confirmado que es un comportamiento propio de `QuickGrid` de Microsoft: rellena el `<tbody>` con filas `<tr><td></td></tr>` vacías hasta completar `ItemsPerPage` (20), incluso sin `Virtualize="true"` y con un `ItemsProvider` que ya devuelve solo los elementos reales — no era un bug de este código. Arreglado con una regla CSS en `list-page.css` que oculta las filas donde ninguna celda tiene contenido (`tr:not(:has(td:not(:empty)))`). Verificado en vivo: filtrando a 1 resultado, quedaba 1 fila visible de las 20 en el DOM (antes las 19 restantes se veían como líneas vacías).

5. **Selectores de Cliente/Empresa en el drawer de Subcontrata necesitan buscador + paginación**: con cientos de Clientes/Empresas (como ahora, gracias al seed de prueba) la lista de checkboxes es inmanejable. Pide: (a) un buscador interactivo tipo texto libre que filtre la lista en vivo, (b) paginación dentro de la lista, y (c) una casilla "Solo mostrar clientes relacionados" que, al marcarla, oculta todo lo que no esté ya seleccionado (`_clienteIdsSeleccionados`/`_empresaIdsSeleccionados` en `Subcontratas.razor.cs`). Aplica igual a la lista de Empresas del mismo drawer, y al patrón gemelo en `Empresas.razor` (lista de Clientes).
    - **Hecho y verificado en navegador (2026-07-16)**: extraído un componente nuevo `SelectorMultiple.razor` (`CaeManager.Web/Components/DesignSystem/`) — buscador con debounce (reutiliza `CampoTexto`), casilla "Solo mostrar relacionados" y paginación en memoria de 20 en 20 (reutiliza `PaginadorSimple`, ya existente para Auditoría/Usuarios/Reportes). Sustituye el `@foreach` manual de checkboxes en los tres sitios: Empresas→Clientes, Subcontratas→Clientes y Subcontratas→Empresas. El componente recibe una lista plana (`ElementoSeleccionable(Id, Nombre)`) para no acoplarse a los DTOs concretos de cada pantalla. Verificado con Playwright: filtra en vivo (200→4 al buscar "Alicante Facility"), "solo relacionados" reduce a los ya marcados, el paginador aparece con las 200 filas sin filtrar y funciona ("Página 1 de 10"), y no rompe el fix de filas vacías del punto 4. Ajustado el CSS para que la casilla no se corte en el ancho fijo del drawer (420px) — pasa a su propia línea en vez de recortarse.

6. **Centros: el selector de Cliente no despliega la lista, y Empresa depende de elegir Cliente primero**. Dos cosas a separar: (a) posible bug real — confirmar si `ObtenerClientesParaSelectorQuery` no está devolviendo datos en esa pantalla o si es un problema de binding en el `CampoSelect`; (b) cambio de diseño — hoy `ObtenerEmpresasParaSelectorQuery(ClienteId)` filtra Empresas por Cliente ya elegido (cascada obligatoria). El usuario dice que un Centro siempre pertenece a una Empresa pero no necesariamente a un único Cliente de forma dependiente — aclarar si esto implica permitir elegir Empresa sin Cliente previo, o si además cambia el modelo de Centro (hoy `ClienteId` es un FK obligatorio único, no muchos-a-muchos). Requiere una decisión de producto antes de tocar `Centro.cs`.
   - **Decisión (2026-07-16)**: solo arreglar el selector/UI. `Centro.cs` no cambia — `ClienteId`/`EmpresaId` siguen como FKs obligatorias independientes en el dominio. La cascada Cliente→Empresa es un problema de la pantalla `Centros.razor`, no del modelo.
   - **Hecho y verificado en navegador (2026-07-16)**: eliminada la cascada — `Centros.razor.cs` ahora carga `_clientesDisponibles` y `_empresasDisponibles` (esta última sin filtrar, `ObtenerEmpresasParaSelectorQuery()` sin `ClienteId`, igual que ya se hacía en Trabajadores) a la vez en `AbrirCrearAsync`, y ambos `CampoSelect` quedan habilitados e independientes desde el principio. Con datos de prueba sembrados localmente (200 Clientes, 220+ Empresas) el selector de Cliente carga las 200 opciones y el de Empresa las 220+ sin depender de Cliente — confirmado con Playwright que no hay ningún bug de carga en este código; la hipótesis de que el reporte original coincidía con el punto 1 (sin datos de prueba sembrados en Railway) queda reforzada, ya que aquí con datos sembrados el selector funciona correctamente.

7. **Drawers se cierran solos al seleccionar texto con el ratón**: si se hace clic y arrastra para seleccionar texto dentro del drawer y se suelta el clic fuera de él, el drawer se cierra (el handler de "clic fuera" solo mira dónde termina el `mouseup`, no dónde empezó el `mousedown`). Bug de UX puro, corregible sin tocar el modelo — revisar el componente `Drawer` en `CaeManager.Web/Components/DesignSystem/`.
    - **Hecho y verificado en navegador (2026-07-16)**: `Drawer.razor` ahora rastrea si el `mousedown` empezó en la superposición o en el panel (`_mouseDownEnSuperposicion`, con `@onmousedown:stopPropagation` en el panel) y solo cierra si el gesto completo (mousedown + click) empezó fuera. Verificado con Playwright: arrastrar para seleccionar texto dentro del panel y soltar el clic fuera ya no cierra el drawer; un clic normal (mousedown y up ambos fuera) sí lo sigue cerrando.

8. **Feature grande — drawers como "borrador" persistente**: el usuario quiere que los formularios laterales de creación/edición **no se cierren nunca** al navegar por el menú izquierdo u otras pantallas — deben sobrevivir la navegación entre páginas y solo cerrarse con los botones explícitos de Cancelar/Guardar, o al cerrar sesión. Esto es un cambio arquitectónico serio: hoy cada drawer vive como estado local del componente de la página (`_drawerVisible` en cada `*.razor.cs`), que se destruye al navegar a otra ruta en Blazor Server. Implica un servicio global de "borrador activo" (similar a `ToastService`/`BusquedaGlobalService`) que sobreviva la navegación y un `Drawer` renderizado fuera del árbol de cada página (p. ej. en `MainLayout`). Diseñar con cuidado antes de implementar — afecta a todos los módulos con drawer. ([Issue #1](https://github.com/christopherjp1-jpg/Project-Hydra/issues/1))
   - **Decisión (2026-07-16)**: piloto en un solo módulo primero (Empresas), validar guardado/cancelado/cierre de sesión, y solo después extenderlo al resto de módulos con drawer.

9. **Feature grande — Documentos a nivel Cliente y a nivel Empresa, no solo Trabajador**: hoy `Documento` (`CaeManager.Domain/Documentos/Documento.cs`) exige obligatoriamente un `TrabajadorId`. El usuario necesita: (a) tipos de documento que son del **Cliente**, no de un empleado — ejemplos reales: RLC (Registro de Licitadores y Empresas Clasificadas), ITA, RNT; y (b) documentos específicos de una **Empresa** en concreto, como formularios personalizados de higiene. Esto requiere repensar `Documento` para que apunte opcionalmente a Trabajador, Cliente o Empresa (patrón similar al que se acaba de hacer con `Trabajador.EmpresaId`/`SubcontrataId`, pero con tres niveles en vez de dos) y probablemente una nueva dimensión en `TipoDocumento` (`AmbitoAplicacion`: Trabajador/Cliente/Empresa). Fase grande — diseñar el modelo antes de tocar código.
   - **Decisión (2026-07-16)**: tres FKs opcionales excluyentes en `Documento` (`TrabajadorId?`, `ClienteId?`, `EmpresaId?`, exactamente una rellena) + nueva propiedad `AmbitoAplicacion` (Trabajador/Cliente/Empresa) en `TipoDocumento`, mismo patrón que `Trabajador.EmpresaId`/`SubcontrataId`.
   - **Hecho y verificado en navegador (2026-07-16)**: implementación completa.
     - **Dominio**: `Documento` pasa a tener `TrabajadorId?`/`ClienteId?`/`EmpresaId?` con constructor privado + tres factories estáticas (`DeTrabajador`/`DeCliente`/`DeEmpresa`, mismo patrón que `Trabajador.DeEmpresa`/`DeSubcontrata`) y una propiedad computada `Ambito`. `TipoDocumento` gana `AmbitoAplicacion` (enum nuevo `CaeManager.Domain.Documentos.AmbitoAplicacion`), fijo desde la creación (no editable después — evita que un tipo ya usado cambie de ámbito y deje Documentos inconsistentes). 12 tests de dominio nuevos.
     - **Aplicación**: `CrearDocumentoCommand` generaliza a tres FKs opcionales con regla XOR y valida que el ámbito del TipoDocumento elegido coincide con el propietario (p. ej. no se puede crear un "Apto médico laboral" contra un Cliente). `ObtenerDocumentoPorIdQuery` resuelve el nombre del propietario según cuál FK esté rellena. `ObtenerDocumentosQuery` (listado paginado de `/documentos`) une los tres ámbitos con `Concat` de tres sub-queries (una por ámbito, cada una con su propio join) en vez de un LEFT JOIN triple, y admite filtrar por ámbito. `ObtenerTiposDocumentoQuery` admite filtrar por ámbito para poblar el selector de Tipo de documento según lo elegido. Alertas/Calendario/Dashboard (KPIs y desglose)/Reportes se acotan explícitamente a Documentos de Trabajador (con nota en el código) — **extenderlos a Cliente/Empresa queda fuera de alcance de esta fase**, decisión para no disparar el tamaño del cambio.
     - **Infraestructura**: migración `AgregarAmbitoDocumentos` — columna `AmbitoAplicacion` en `TiposDocumento` (con backfill a "Trabajador" para los 15 tipos semilla existentes) y `TrabajadorId` pasa a nullable + nuevas columnas `ClienteId`/`EmpresaId` en `Documentos`. Revisada la SQL generada (`dotnet-ef migrations script`): SQLite reconstruye la tabla `Documentos` con `INSERT INTO ... SELECT ...` antes de sustituirla, sin pérdida de datos.
     - **Web**: `/tipos-documento` añade un selector "Ámbito" al crear (fijo al editar) y oculta "Centros donde aplica" para ámbitos que no son Trabajador (no tiene sentido asociar a Centro un documento de Cliente/Empresa). `/documentos` añade un selector "¿A quién pertenece?" en el alta que cambia el selector de propietario (Trabajador/Cliente/Empresa) y filtra el catálogo de Tipo de documento en consecuencia; la tabla cambia la columna "Trabajador" por "Propietario" + una columna "Ámbito", y suma un filtro de Ámbito en la barra de filtros.
     - **Verificado con Playwright**: creado un TipoDocumento "RLC" (ámbito Cliente) y "Formulario higiene" (ámbito Empresa); creado un Documento de cada uno contra un Cliente/Empresa real; el listado filtra correctamente por ámbito (1 resultado al filtrar por Cliente); el flujo original de Documento de Trabajador se probó de nuevo end-to-end sin cambios de comportamiento; Dashboard/Alertas/Calendario/Reportes/Tipos de Documento/Trabajadores cargan sin errores tras el cambio. 90/90 tests en verde.

10. **Conversión de archivos no-PDF es lenta y permite guardar antes de terminar**: al subir un archivo que no es PDF (se convierte automáticamente, ver Fase 16), si el usuario pulsa Guardar antes de que la conversión termine, el Documento se guarda sin archivo adjunto activo. Dos frentes: (a) bloquear el botón Guardar mientras la conversión esté en curso (fix de UX, más simple e independiente); (b) investigar por qué la conversión es lenta comparada con un servicio como iLovePDF, y si vale la pena evaluar LibreOffice (`soffice --headless --convert-to pdf`) en vez del conversor actual — revisar qué librería se usa hoy para JPG/PNG→PDF (Fase 16) antes de decidir.
    - **(a) Hecho (2026-07-16)**: ver commit del bug del botón Guardar — `Documentos.razor` ahora deshabilita Guardar mientras `_subiendoArchivo` es true.
    - **(b) Investigado, sin cambio de código (2026-07-16)**: revisado `ConversorArchivosPdf.cs` (`CaeManager.Web/Documentos/`) — hoy solo acepta JPG/PNG (no PDF de verdad "no-PDF" en sentido amplio, no hay Word/Excel), y la conversión es `PdfSharp` dibujando la imagen en una página, en el mismo proceso .NET — no debería ser lenta en sí misma para fotos de tamaño normal. Probado en este entorno: `soffice --headless --convert-to pdf` tarda **~5-6 segundos solo en arrancar el proceso** (carga de fuentes/framework de LibreOffice), incluso antes de convertir nada — sustituir PdfSharp por LibreOffice empeoraría la lentitud percibida, no la arreglaría, para este caso de uso concreto (imagen→PDF en un proceso ya vivo). Si la lentitud reportada es real, el sospechoso más probable es el tamaño del archivo en sí (fotos de móvil sin comprimir, varios MB, subida por red real en Railway) o el tiempo de `AlmacenamientoArchivos.GuardarAsync` guardando el PDF resultante — no la librería de conversión. Recomendación: medir con una foto real de móvil antes de tocar nada más aquí.
    - **Hecho y verificado en navegador (2026-07-16)**: parte (a) — el botón Guardar de `Documentos.razor` ahora usa `Deshabilitado="_subiendoArchivo"` (antes solo `Cargando="_guardando"`, que no cubría la fase de conversión). Verificado con Playwright subiendo un JPG real: el botón queda `disabled` durante la ventana de conversión/subida y vuelve a habilitarse justo después. Parte (b) (investigar LibreOffice) sigue sin hacer.

11. **Inputs de texto con sensación de lag al escribir rápido**: probable causa — cada tecla dispara un round-trip al servidor por el circuit de SignalR (binding `ValorChanged` de `CampoTexto` en cada `@oninput`), y bajo Railway (latencia de red real, no localhost) esto se nota como si el sistema "autocorrigiera" con retraso. Solución típica: debounce en el cliente antes de notificar al servidor, o cambiar el binding de esos campos a `@bind:event="onchange"` en vez de `oninput` donde no haga falta reactividad por tecla. Confirmar primero si de verdad es esto midiendo el round-trip.
    - **Hecho y verificado en navegador (2026-07-16)**: añadido debounce de 300 ms dentro de `CampoTexto.razor` — el `oninput` sigue disparando el round-trip (inevitable en Blazor Server), pero ya no se llama a `ValorChanged.InvokeAsync` hasta que el usuario hace una pausa al escribir. Esto evita el efecto "autocorrección": antes, si la respuesta de una tecla anterior llegaba tarde bajo latencia real, Blazor volvía a fijar el atributo `value` del `<input>` con un valor desactualizado, pisando visualmente lo que el usuario ya había escrito después. **Regresión encontrada y corregida durante la verificación con Playwright**: al probar el flujo real de "escribir y pulsar Guardar" se descubrió que un clic rápido tras escribir podía llegar al servidor antes que el debounce, enviando la Razón social vacía y bloqueando el alta con "La razón social es obligatoria" pese a verse texto en el campo. Se añadió `@onblur` en `CampoTexto.razor` que cancela el debounce pendiente y notifica el valor ya mismo al perder el foco (ocurre antes que el clic en Guardar en el orden de eventos del navegador). Verificado de nuevo: escribir rápido y guardar ya no falla, y la búsqueda en vivo sigue filtrando correctamente escribiendo a máxima velocidad.

12. **Feature — email de bienvenida al crear Usuario**: al dar de alta un Usuario (`/usuarios`) debe enviarse un correo con el link de acceso y las credenciales. Pedido explícito: dejar una plantilla básica ahora, diseño definitivo más adelante. Requiere infraestructura de envío de correo que **no existe todavía en el proyecto** (sin `IEmailService` ni configuración SMTP) — hay que elegir proveedor/mecanismo (SMTP directo, SendGrid, etc.) y añadirlo a `DEPLOY.md` como nueva variable de entorno, siguiendo el mismo patrón que `DataProtection`/`AdministradorInicial`. ([Issue #2](https://github.com/christopherjp1-jpg/Project-Hydra/issues/2))
    - **Nota (2026-07-16)**: ProjectHydra ya paga licencias de Microsoft 365/Outlook — antes de dar de alta un proveedor nuevo (SendGrid, etc.), evaluar Microsoft Graph API (`sendMail`) o SMTP de Exchange Online con esas mismas licencias, para no pagar dos veces por envío de correo. Pendiente de implementar — no se tocó en esta sesión.

13. **Bug: plantilla descargable no aparece en `/importacion`**: la página de importación masiva debería ofrecer un enlace/botón para descargar la plantilla Excel (Fase 5/18) y no está apareciendo. Reproducir y revisar `Importacion.razor` y los servicios `IPlantillaClientesService`/`IPlantillaCombinadaService`/`IPlantillaDocumentosService` (¿el endpoint de descarga está registrado? ¿el botón está condicionado a algo que ya no se cumple?).
    - **Investigado y verificado en navegador (2026-07-16)**: no era un bug de código — `/importacion` (Fase 5, Cuadro de Control CAE) nunca tuvo plantilla en blanco a propósito, porque procesa un archivo ya existente del cliente KHS (ver "Fuera de alcance de v1"). Las plantillas descargables sí existen y funcionan (`/clientes/plantilla.xlsx`, `/clientes/plantilla-combinada.xlsx`, `/documentos/plantilla.xlsx`, con sus endpoints registrados en `ClientesEndpoints.cs`/`DocumentosEndpoints.cs`), pero solo son accesibles desde enlaces en `/clientes` y `/documentos` — el menú lateral (`NavMenu.razor`) solo enlaza a `/importacion`, así que un usuario que entra por "Importar desde Excel" nunca las descubre. Añadido un enlace de descubrimiento en `Importacion.razor` hacia `/clientes/importar` y `/clientes/importar-combinado`, confirmado visible en navegador.

14. **Feature — widget de notificaciones diarias**: durante la ronda de mejoras visuales de 2026-07 (ver `DESIGN_SYSTEM.md` → "Micro-interacciones") el usuario confirmó que quiere este widget, pero no para esa ronda: "no por ahora pero añádelo en el roadmap y pendiente de definir bien". El prototipo de referencia aportado (`EfCoreModels.cs`) usa una entidad `TareaPendiente` con reinicio/racha diaria, que **no tiene equivalente en el dominio actual de CAE Manager** — no es un ajuste visual, es una feature de producto nueva. Pendiente de definir antes de diseñar o implementar: ¿qué notifica exactamente? (¿documentos que vencen próximamente, tareas manuales asignadas a un usuario, ambos?), ¿es informativo (solo lectura) o accionable (marcar como hecho, con racha/reinicio diario como el prototipo)?, ¿vive en el Dashboard o es un panel/dropdown propio en la barra superior?, ¿por perfil de usuario o global? ([Issue #3](https://github.com/christopherjp1-jpg/Project-Hydra/issues/3))

## Backlog pendiente — reportado por el usuario el 2026-07-19, sin dimensionar ni implementar

⏳ Sin empezar. Bloque de seguridad, observabilidad de producto y onboarding, pedido explícitamente para retomar en sesiones siguientes (objetivo: cerrar antes del jueves). Ninguno de estos puntos se ha investigado ni dimensionado todavía — el primer paso de cada uno, antes de escribir código, es diagnosticar el estado real (qué ya existe, qué falta) y solo entonces implementar.

1. **Seguridad — marco general**: aplicar los principios OWASP (Top 10) y la tríada CIA de ISO 27001 (Confidencialidad/Integridad/Disponibilidad) de forma sistemática, y asegurar que cada tabla nueva que se genere tenga RLS (Row-Level Security) activado. Nota de diagnóstico: hoy el aislamiento por cartera se hace a nivel de aplicación (`IAlcanceDatosService`, Fase 31/Issue #18), no a nivel de motor de base de datos — SQLite no tiene RLS nativo como Postgres, así que hay que decidir si esto implica migrar a Postgres (ver "Fuera de alcance de v1") o formalizar el equivalente a nivel de aplicación con la misma garantía.
2. **Auditoría de vulnerabilidades de código**: revisión completa del código, explicación en lenguaje sencillo de cada vulnerabilidad encontrada y el código concreto para solucionarla.
3. **Auditoría de rutas de API**: revisar todos los endpoints (minimal APIs de `*Endpoints.cs` — descargas de plantillas, callback de SSO, etc. — y todo lo que pase por MediatR) e identificar cuáles no tienen validación o autenticación, con el código necesario para protegerlos.
4. **Auditoría de entradas de usuario**: revisar todos los puntos donde la app recibe datos del usuario (formularios, importación Excel, subida de archivos, chat IA) y evaluar riesgo de inyección o datos maliciosos.
5. **Analítica de producto**: instalar una herramienta de analítica con mapa de calor (heatmap) y métricas de usabilidad por usuario, por funcionalidad y por botón, para detectar puntos de mejora de UX. Requiere elegir proveedor (p. ej. algo autoalojable por RGPD/Fase "Iniciativa de hardening", o un SaaS con acuerdo de tratamiento de datos) antes de integrar nada.
6. **Onboarding de la plataforma**: flujo guiado para un usuario nuevo (probablemente ligado a los roles de la Fase 31 y a la sala de espera SSO de la Fase 34) — sin definir todavía qué pantallas cubre ni si es obligatorio u omitible.
7. **Rate limiting**: limitar peticiones por usuario — evaluar `Microsoft.AspNetCore.RateLimiting` (ya integrado en ASP.NET Core, sin dependencias nuevas) y decidir qué límites aplican a qué rutas (login, API, subida de archivos).
8. **Cooldown de intentos fallidos de login**: proteger contra fuerza bruta con bloqueo temporal tras varios intentos erróneos. Nota de diagnóstico: ASP.NET Core Identity ya trae bloqueo de cuenta configurable (`Lockout`) — revisar si está activado hoy en `InfrastructureServiceCollectionExtensions.AddIdentityCore` (a simple vista no se ha configurado explícitamente en este repositorio) antes de construir nada desde cero.
9. **Endurecer el request de login**: asegurar que el flujo de login (local y SSO) no sea vulnerable a ningún tipo de ataque — probablemente se solape con los puntos 2/3/8 de este mismo bloque, no tratar como algo aparte sin antes cruzarlo con esos hallazgos.
10. **Informe de estado de la plataforma** (para mostrar avance la semana del 2026-07-20): punteo del estado actual, listado de funcionalidades/casos de uso implementados vs. pendientes, listado de APIs/servicios creados, funcionalidades bloqueantes vs. no bloqueantes, bugs/errores pendientes, y acciones bloqueadas o dependientes de otra cosa por funcionalidad. Ver el informe ya entregado el 2026-07-19 como primera versión — actualizarlo antes de mostrarlo si hay cambios entre ahora y esa fecha.

## Fuera de alcance de v1 (explícito, no implica "nunca")

- ~~Multi-tenant real~~ **Ya no está fuera de alcance.** **Historial de la decisión**: fuera de alcance en v1 → revertido el 2026-07-17 hacia SaaS multi-cliente (`ADR-001-multitenant.md`) → en pausa el 2026-07-18 (`ADR-002-single-tenant.md`, uso interno + cláusula de fork) → **reactivado el 2026-07-23 (`ADR-003-saas-multitenant.md`)**: la vía SaaS multi-tenant es el objetivo principal del producto, implementación in-place en este repositorio con la organización actual como tenant #1. Ver sección "Iniciativa de hardening" → punto 3, `docs/MULTITENANCY.md` e `INFORME-MULTITENANT.md`.
- Migración a PostgreSQL/SQL Server (arquitectura preparada, no ejecutada). **Actualización 2026-07-23**: pasa de "cuando el volumen lo justifique" a **condición de salida a producción SaaS** (`ADR-003` — SQLite no sostiene escritura concurrente multi-organización).
- CIF/Empresa en las plantillas de importación por Excel de Fase 5 (Cuadro de Control CAE de KHS) y Fase 7 (plantilla simple de Clientes) — ninguna de las dos se extendió; siguen sin poder crear Clientes/Centros nuevos. La Fase 18 resuelve esta limitación con una plantilla combinada nueva y separada, no modificando las dos anteriores.
- Exigencia real de Tipos de Documento por ámbito: la Fase 15 solo filtra la lista de `/tipos-documento` — el selector de Tipo de Documento al crear un Documento sigue mostrando el catálogo completo, y no existe (todavía) una alerta de "documento faltante" para un Trabajador de un Centro que exige un Tipo que no tiene. Decisión explícita del usuario al acotar el alcance de esa fase.
- Purga automática de archivos antiguos tras un periodo configurable (Fase 17 solo permite verlos, no los borra) — exigiría la primera infraestructura de tareas programadas del proyecto; decisión explícita del usuario al acotar el alcance de esa fase.
