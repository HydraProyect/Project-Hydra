namespace CaeManager.Infrastructure.Identity;

/// <summary>Preferencia de tema del usuario. "Sistema" sigue el prefers-color-scheme del sistema operativo.</summary>
public enum TemaPreferido
{
    Sistema = 0,
    Claro = 1,
    Oscuro = 2
}
