namespace CaeManager.Web.Components.DesignSystem;

/// <summary>Una pestaña de <see cref="Pestanas"/> — Id estable (usado en la URL/estado), Etiqueta visible.</summary>
public record PestanaDefinicion(string Id, string Etiqueta);
