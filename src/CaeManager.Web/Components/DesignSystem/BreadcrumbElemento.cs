namespace CaeManager.Web.Components.DesignSystem;

/// <summary>Un cruce de <see cref="Breadcrumb"/> — solo texto, el componente no sabe nada de dominio.</summary>
public record BreadcrumbElemento(string Etiqueta);
