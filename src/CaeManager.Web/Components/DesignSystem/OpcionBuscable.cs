namespace CaeManager.Web.Components.DesignSystem;

/// <summary>Una opción de <see cref="CampoBuscarSelect"/> — Texto es lo que el usuario ve y escribe, Id es lo que se notifica en ValorChanged al coincidir exactamente.</summary>
public record OpcionBuscable(string Id, string Texto);
