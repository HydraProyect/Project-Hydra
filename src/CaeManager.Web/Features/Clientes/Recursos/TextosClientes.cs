namespace CaeManager.Web.Features.Clientes.Recursos;

/// <summary>
/// Marcador de <c>IStringLocalizer&lt;TextosClientes&gt;</c>: los textos de la
/// Feature Clientes ya migrados a recursos. <c>TextosClientes.resx</c> es el
/// neutral (español) y <c>TextosClientes.ca-ES.resx</c> el catalán, con las
/// mismas claves (<c>LocalizacionRecursosYRegistroTests</c>, Architecture.Tests).
///
/// <para>
/// La Feature no está migrada entera: entra aquí el texto nuevo o reescrito
/// (la página Cliente 360 y el rótulo «Cliente» de su panel), y el resto sigue
/// contando en <c>TextosSinLocalizarCongeladosTests</c> hasta su incremento de
/// migración.
/// </para>
/// </summary>
public sealed class TextosClientes;
