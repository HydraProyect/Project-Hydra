namespace CaeManager.Web.Features.Empresas.Recursos;

/// <summary>
/// Marcador de <c>IStringLocalizer&lt;TextosEmpresas&gt;</c>: los textos de la
/// Feature Empresas ya migrados a recursos. <c>TextosEmpresas.resx</c> es el
/// neutral (español) y <c>TextosEmpresas.ca-ES.resx</c> el catalán, con las
/// mismas claves (<c>LocalizacionRecursosYRegistroTests</c>, Architecture.Tests).
///
/// <para>
/// La Feature no está migrada entera: entra aquí el texto nuevo o reescrito
/// (la página Empresa 360 y la «Vista rápida» del menú de fila de la lista), y
/// el resto sigue contando en <c>TextosSinLocalizarCongeladosTests</c> hasta su
/// incremento de migración.
/// </para>
/// </summary>
public sealed class TextosEmpresas;
